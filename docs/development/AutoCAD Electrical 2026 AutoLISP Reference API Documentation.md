# AutoCAD Electrical 2026 AutoLISP Reference API Documentation

Do not edit this file manually. Regenerate it from the installed Autodesk CHM help.

## Source Authority

- This document is generated from the installed Autodesk AutoCAD Electrical help, not maintained by hand.
- Generated at: 2026-04-02T15:15:42.535Z
- CHM source: `C:/Program Files/Autodesk/AutoCAD 2026/Acade/Help/en-US/Help/ACE_API.chm`
- Extracted root: `C:/Users/DEV~1/AppData/Local/Temp/suite-ace-api-chm`
- TOC source: `AutoLISP Reference.hhc`
- Coverage: 12 API sections, 275 routine topics, 299 total TOC entries.

## Scope

- Capture the installed AutoCAD Electrical 2026 AutoLISP/API reference from Autodesk's local `ACE_API.chm`.
- Preserve the shipped section and routine catalog so Suite can reason from the workstation's local authority instead of guessing from generic web summaries.
- Record the adjacent support, lookup-database, and template assets under the `Acade` install root that may matter to Suite automation work.

## What's New

This topic outlines the changes made to the AutoCAD Electrical APIs for this release:

- No changes were made to the API this release

## Introduction

The AutoCAD Electrical 2026 API entry point list consists of a series of entry points into the AutoCAD Electrical software executable. This API allows a user to create custom applications such as automatic schematic generation or custom drafting/design utilities based upon the AutoCAD Electrical "engine".

The AutoCAD Electrical 2026 API is AutoLISP based but it isn't limited to this development environment. It can be invoked from VB.NET, C#, C++/ARX, VBA, AutoCAD script files, or embedded into custom AutoCAD menus.

The following example illustrates the same custom command created in AutoLISP, C++/ObjectARX, VB.NET, and C#. This example makes calls to two of AutoCAD Electrical's API routines:

This program is to map a color suffix to each wire number based upon the name of the wire's LINE layer name. For example, if wire number "101" is on a LINE wire drawn on layer name 14AWG-RED-THHN then this utility applies a "red" suffix to the wire.

AutoLISP version

C++ ObjectARX version

Visual Basic version

C # version

AutoCAD Electrical's spreadsheet to PLC I/O utility can generate a set of PLC I/O drawings from data pulled from a Microsoft Excel spreadsheet. This command is supplied in uncompiled source code form (and also executed from this uncompiled form) to serve as an example of a large, intricate application that makes heavy use of the API.

The program file is wdio.lsp (and companion dialog definition file wdio.dcl). The default install location for these two files is "c:\Documents and Settings\All Users\Documents\Autodesk\acade {version}\support\". You might use a copy of this program file as a model for creating your own custom drawing generation application.

The following AutoLISP utility can be referenced by the title block mapping file (the ".wdt" file) or encoded into your title block's "WD_TB" invisible attribute value (if that's the method you use to link your title block to AutoCAD Electrical).

When this utility runs, it returns the last wire number it finds on the drawing. If no wire numbers found then it returns blank. It excludes wire numbers that are associated with wire networks having "Destination" wire signal arrows on them (meaning that the actual wire number assignment was made on some other drawing).

### Linked Autodesk Topics

- ace_get_wnum - find and return the wire number associated with a given wire line entity.
- wd_putwn - write a new wire number value back out to a wire "network".

### Source Topics

- `c_ace_get_wnum.html` (ace_get_wnum)
- `c_wd_putwn.html` (wd_putwn)
- `AutoLISP_sample_code.html` (AutoLISP version)
- `ObjectARX_code_samples.html` (C++ ObjectARX version)
- `Visual_Basic_sample_code.html` (Visual Basic version)
- `C-sharp_(C_)_sample_code.html` (C # version)
- `AutoLISP_code_samples_(PLC).html` (AutoLISP version)

## Local Installation Assets Relevant to Automation

- Install root: `C:\Program Files\Autodesk\AutoCAD 2026\Acade`
- Help root: `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Help\en-US\Help`
- API CHM: `C:/Program Files/Autodesk/AutoCAD 2026/Acade/Help/en-US/Help/ACE_API.chm`
- Compiled runtime modules: 161 `.fas` files are present in the install root; treat those as shipped binaries, while the `.lsp` and `.dcl` files remain the inspectable support sources.

### Editable Support Sources

- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\wd_load.lsp`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Support\en-US\Shared\wdio.lsp`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Support\en-US\Shared\wdio.dcl`

### Lookup Databases

- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\en-US\DB\ace_plc.mdb`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\en-US\DB\default_cat.mdb`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\en-US\DB\footprint_lookup.mdb`

### Template and Seed Roots

- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\en-US\Electrical`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\en-US\Template`

### Demo Project Seeds

- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\Aegs`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\Demo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\Extra Library Demo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\GbDemo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\Iec60617Demo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\IecDemo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\IeeeDemo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\JisDemo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\NfpaDemo`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\My Documents\Acade 2026\AeData\Proj\Point2Point`

### User Support Payload

- Root: `C:\Program Files\Autodesk\AutoCAD 2026\Acade\UserDataCache\en-US\Electrical\UserSupport`
- Icon menu definitions: 15 files, including `ACE_AS_MENU.DAT`, `ACE_GB_MENU.DAT`, `ACE_HYD_MENU.DAT`, `ACE_IEC-60617_MENU.DAT`, `ACE_IEC_MENU.DAT`, `ACE_IEEE_MENU.DAT`.
- Symbol/menu library payloads: 34 `.slb/.dll` files in the same root.
- Workspace support files include `acade.cuix`, `acade.fmp`, `acade.lin`, `acadepidshp.shx`, `acadSynonymsGlobalDB.pgp`, `AcCommandWeight.xml`.

### Shipped Sample Drawings

- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\3-phase motor control, Control circuit.DWG`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Control Schematic.DWG`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Control Wiring.DWG`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Emergency Stop Circuit.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Main Cabinet Layout.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Main Control Panel layout.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Main Drive.DWG`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Operator Station layout 1.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Operator Station layout 2.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\P&ID Diagram.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\PLC IO Rack Arrangement (1746-A7).DWG`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Point to Point.dwg`
- `C:\Program Files\Autodesk\AutoCAD 2026\Acade\Sample\Safety Circuit.dwg`

## Section Catalog

### Section A - Schematic Components

Insert Component, Break Wire, and Auto Tag Generation.

- Source topic: `Section_A_-_Schematic_Components.html`
- Routine count: 30

- `c:ace_iec_tag_freshen1` - Trigger reformat of TAG1/TAG2/TAGSTRIP/P_TAG1 attribute text. Signature: `(c:ace_iec_tag_freshen1 param_lst)`. Parameters: `param_lst` = (list ben options) where: "ben" = component or panel footprint entity to update."options" = 4's bit set = force INST/LOC attribs to be VISIBLE, 8's bit set = force INST/LOC attribs to be INVISIBLE.. Returns: 1 if something changed, nil otherwise. Source: `c_ace_iec_tag_freshen1.html`.
- `c:ace_ins_parametric_connector` - Insert parametrically generated multi-pin schematic connector symbol. Signature: `(c:ace_ins_parametric_connector xy scl param_lst)`. Parameters: `xy` = Insertion point. nil = prompt for insertion point.; `scl` = nil = default scale (follows global GBL_wd_scl_plcbld); `param_lst` = List of parameters nth 0 : connector type (1=plug/recpt, 2= plug/recept (2 blocks), 4=plug only, 8=receptacle only)nth 1 : show pin numbers - plug/jack: 2 = both, 4 = plug side, 8 = recpt side, 16 = neither side, single plug or single jack insert: 0=hide, 1=shownth 2 : 1 = vertical insert, 2 = horizontalnth 3 : 1 = plug side on right or top, 2 = left or bottomnth 4 : 1 = add divider line if plug/jack insert, 0 = no divider linenth 5 : 1 = wire number changes through plug/jack symbolnth 6 : 1 = wire number changes through plug/jack symbolnth 7 : 1 = fixed pin spacing, 2 = stretch to match wire crossingsnth 8 : 1 = insert all, 2 = prompt spacers/breaksnth 9 : pin spacing (ex: 0.75)nth 10 : receptacle side widthnth 11 : plug side widthnth 12 : top extra heightnth 13 : bottom extra heightnth 14 : plug corner radius valuenth 15 : pin countnth 16 : starting pin number text string (or comma-delimited string of specific pin assignments, like "A,B,1,2")nth 17 : 1 = start insert as a "child" connector, 0 = parentnth 18 : 1 = start with "break" line, 0 = normal. Returns: if fail, otherwise list of entity names of connector or connector pieces that inserted Example topic: `_c_ace_ins_parametric_connector_3_Example.html`. Source: `c_ace_ins_parametric_connector.html`.
- `c:ace_lastent_list_from` - Get list of recent entities inserted into the active drawing. Return list of entities inserted into the active drawing database after "from_ent" was inserted. Signature: `(c:ace_lastent_list_from from_ent options)`. Parameters: `from_ent` = The return value from (c:ace_lastent_mark) or nil=start at beginning of database.; `options` = 1's bit set = return all entities, even subentities.1's bit clear = return only block INSERT entities.2's bit set = return as list of "handles" (clear = return list of entity names).. Returns: List of handles or entity names (nil=none found). Example topic: `_c_ace_lastent_list_from_3_Example.html`. Source: `c_ace_lastent_list_from.html`.
- `c:ace_lastent_mark` - Find the last entity or subentity in the active drawing. Use with (c:ace_lastent_list_from...) Signature: `(c:ace_lastent_mark)`. Returns: Last entity name (or nil if drawing is empty). Example topic: `_c_ace_lastent_mark_3_Example.html`. Source: `c_ace_lastent_mark.html`.
- `c:ace_multipole` - Insert multipole symbol. The spacing between poles is determined by intersection with underlying wires or, if none found, by the drawing's default ladder rung spacing (carried on attribute RUNGDIST of the drawing's WD_M block insert which then becomes accessible as the 11th element (index 0 based) of the drawing's GBL_wd_m configuration global variable). Signature: `(c:ace_multipole symname polecnt xy options)`. Parameters: `symname` = Symbol to insert.; `polecnt` = Total pole count (nil, 1, 2 =2 pole). 2 pole is minimum inserted.; `xy` = insertion point for first symbol, nil=user prompt; `options` = bit 1 set means suppress edit dlg after insert (edits first component inserted)bit 2 set means force default to start with "H" symbol orientation (horiz wire insert)bit 4 set means force default to start with "V" symbol orientation (vertical wire insert)bit 2+4 blank = no forced defaultbit 8 set means force build vertical wire insertion to the LEFT or horizontal wire insertion UPbit 16 set means force build vertical wire insertion to the RIGHT or horizontal wire insertion DOWNbit 8+16 blank means prompt user for direction.bit 64 suppress any prompt for user pick on child wire (if wire not found)bit 128 set means that all LINEs are viewed as valid wires and can be connected to, even if not on a wire layer (used by CIRC BLDR utility). Source: `c_ace_multipole.html`.
- `c:ace_pnl2sch_lookup` - Find schematic component symbol name. For a given panel footprint symbol, attempt to find the equivalent schematic component symbol name. This data is returned by a query on the schematic_lookup.mdb file. Signature: `(c:ace_pnl2sch_lookup ben)`. Parameters: `ben` = a panel footprint block entity name on the active drawing.. Returns: List of sublists with 2 elements each ((symbol_name comment) (symbol_name comment)) or nil if none found. Source: `c_ace_pnl2sch_lookup.html`.
- `c:ace_projwide_util_dbx` - Project-wide update/retag function. Signature: `(c:ace_projwide_util_dbx dist what2dolst datalst options_lst)`. Parameters: `dist` = list of drawings from active project or nil=process all drawings in project; `what2dolst` = A list of up to 27 elements: nth 0 - run_retag = 1 for component retag, 0 to skipnth 1 - run_wire = 1 for wire renumbering, 0 to skipnth 2 - run_xref = 1 for cross-reference update, 0 to skipnth 3 - future, set to 0nth 4 - run_ladr = 1 ladder reference resequence, 0 to skipnth 5 - run_sht = 1 sheet resequence, 0 to skipnth 6 - ladr_mode = "S" for sequential, "B" for bump up/down amountnth 7 - ladr_amt = for "B" above - bump sequencing up or down (integer number)nth 8 - ladr_1st = for "S" above - first ladder number (character string value)nth 9 - ladr_skip = if ladr_mode is "S" amount to skip as an integer numbernth 10- sht_mode = "S" for sequential renumbering, "B" to bump current up/downnth 11- sht_1st = if sht_mode is "S" first sheet numbernth 12- sht_amt = if sht_mode is "B" amount to bump up or down as an integernth 13- ladr_seq_mode = if ladr_mode is "S", 0 for continuous, 1 to skipnth 14- run_%D = 1 to adjust drawing properties Drawing %D value, 0 to skipnth 15- drawing%D = value for aboventh 16- comp_tagmode = drawing properties component retag mode ("S" for sequential, "R" for reference-based)nth 17- comp_tagformat = drawing properties component tag format for aboventh 18- comp_tag_seqstart = drawing properties sequential mode starting value (character string value)nth 19- wnum_mode = drawing properties wire numbering mode ("S" for sequential, "R" for reference- based)nth 20- wnum_format = drawing properties wire numbering format (ex: "%N")nth 21- wnum_seqstart = drawing properties sequential mode starting value (character string value)nth 22- wnum_seq_incr = drawing properties sequential mode increment value (integer)nth 23- cross_ref_fmt_upd = 1 to update drawing properties cross-ref format, 0 to skipnth 24- cross_ref_fmt = value for aboventh 25- inter_cross_ref_fmt_upd = 1 to update inter-drawing cross-ref format, 0 to skipnth 26- inter_cross_ref_fmt = value for above; `datalst` = (list wire_def_lst tb_def_lst) where tb_def_lst is reserved for future use, and where wire_def_lst = list of 8 elements for wire numbering options (set to nil if wire numbering not selected). nth 0 - overwrite = 0 for tag only new, 1 for tag allnth 1 - wire_mode = "S" for sequential, "C" for sequential consecutive, "R" for reference basednth 2 - starting_seq = sequential "S" mode - starting number for drawing (character string)nth 3 - use_spcl = 1 to use format override, nil for no overridenth 4 - w_an_fmt = format override, example: "%N"nth 5 - w_an_fmt_fixed = 1 to insert wire numbers as fixed, nil for normalnth 6 - dosigref = 1 cross-ref signals, 0 to skipnth 7 - freshen = 1 to freshen database for signals, 0 to skip; `options_lst` = For future use.. Source: `c_ace_projwide_util_dbx.html`.
- `c:ace_retag_project` - Re-tag schematic components on all drawings in "dlst" or in entire active project (if "dlst" is nil). This is done in a non-scripting mode. Signature: `(c:ace_retag_project dist options_lst)`. Parameters: `dist` = nil for all dwgs in active project, otherwide = list of full path/dwg names to process. Use double backslashes or single forward slashes.; `options_lst` = For future use.. Example topic: `_c_ace_retag_project_3_Example.html`. Source: `c_ace_retag_project.html`.
- `c:wd_calc_comptag` - Calculate tag-ID for parent schematic component. Signature: `(c:wd_calc_comptag param_lst)`. Parameters: `param_lst` = (list "ben") where "ben" = parent component entity name.. Returns: (blank) if failure, calculated tag-ID string if success. Source: `c_wd_calc_comptag.html`.
- `c:wd_delsym_main` - Erase connected component "ben" and try to reconnect all broken wires and reconcile duplicate wire numbers. Signature: `(c:wd_delsym_main ben)`. Parameters: `ben` = AutoCAD entity name of selected AcadE component.. Source: `c_wd_delsym_main.html`.
- `c:wd_get_pinlist` - Query pin list database and return pin list info. Signature: `(c:wd_get_pinlist mfg cat assycode)`. Parameters: `mfg` = Manufacturer code (optional).; `cat` = Catalog part number (required).; `assycode` = assycode value (optional). Returns: nil if no match found. Source: `c_wd_get_pinlist.html`.
- `c:wd_ins_circ2` - User hook into the "INSERT CIRC" command to insert a collection of prewired components. AcadE automatically retags the inserted components based upon circuit line references or next available sequentials. Signature: `(c:wd_ins_circ2 dwgnam xy scl flgbits)`. Parameters: `dwgnam` = WBlocked circ to insert (i.e. AutoCAD ".dwg" file). If the full path is not given then the normal AcadE and Acad paths are searched.; `xy` = Insertion coordinate (nil=user prompted); `scl` = Insertion scale (default = 1.0); `flgbits` = 1's bit set = preserve "FIXED" wire numbers.2's bit set = keep any SRC arrow symbols.4's bit set = suppress check of component attribute layer assignments.8's bit set = suppress check for symbol reconnection to existing wires.16's bit set = make sure that all LINE entities are moved to valid wire layer.32's bit set = don't blank out tags on orphan contacts that come in with the inserted circuit.64's bit set = save copy of parent's TAG1 non-tagged default value as xdata value WD_VIA_PRETAG. This used as temporary storage for the Circuit Builder utility.128's bit set= adjust insertion point to lie on underlying LINE entity. Returns: No return value. Source: `c_wd_ins_circ2.html`.
- `c:wd_insert_elect_block` - Generic INSERT BLOCK command. Wires do NOT break under the block. Signature: `(c:wd_insert_elect_block sym xy layflg scl)`. Parameters: `sym` = Symbol block name (AcadE's search path is used when full path name is omitted); `xy` = Insertion coordinate - not optional.; `layflg` = nil to keep attribs on existing layer, otherwise move attributes to layers per drawing's CONFIG setting (WD_M block).; `scl` = Insertion scale (default = 1.0). Returns: nil or entity name of successfully inserted block insert. Source: `c_wd_insert_elect_block.html`.
- `c:wd_insym` - User hook into the INS COMPONENT command, command-line prompted. Same as c:wd_insym2 but command prompts for symbol name typed in at command line and an insertion point. All data is supplied on the command line with optional EDIT dialog box popping up after symbol inserts. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insym)`. Returns: ent name or "handle" (see "options") of inserted symbol or nil if fail. Source: `c_wd_insym.html`.
- `c:wd_insym_dlg` - User hook into the INS COMPONENT command, icon menu driven. Same as wd_insym but command triggers icon menu display for component selection, user selects insertion point. The icon menu that pops up is the schematic icon menu that is in memory or is the default schematic icon menu (if the menu isn't already in memory. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insym_dlg)`. Returns: No return value. Source: `c_wd_insym_dlg.html`.
- `c:wd_insym_f` - User hook into the INS COMPONENT command, prompt to select "insert fence" component. User prompted to select "insert fence" component from icon menu. Then user prompted to draw fence. AcadE inserts selected component at intersections between fence and underlying wires. Signature: `(c:wd_insym_f)`. Returns: Ent name of inserted symbol or of deleted symbol if cancel out of the EDIT dialog box. Returns nil if symbol not found (AcadE searches in its normal list of paths for the symbol). Source: `c_wd_insym_f.html`.
- `c:wd_insym_f_same` - User hook into the INS COMPONENT command, user prompted to pick on a "just like" component. Same as wd_insym_f but user prompted to pick on a "just like" component. Then user prompted to draw fence. AcadE inserts selected component at intersections between fence and underlying wires. Signature: `(c:wd_insym_f_same)`. Returns: Ent name of inserted symbol or of deleted symbol if cancel out of the EDIT dialog box. Returns nil if symbol not found (AcadE searches in its normal list of paths for the symbol). Source: `c_wd_insym_f_same.html`.
- `c:wd_insym_go2menu` - Display a submenu page in the current icon menu. Jump to and display the "mnum" submenu page in the current icon menu file. A pick on this menu will feed the selected button's value to the INS COMPONENT command. Signature: `(c:wd_insym_go2menu mnum)`. Parameters: `mnum` = Menu page number to display (i.e. the "**Mx" numbers in the wd_menu.dat file, or whatever menu is current).. Example topic: `_c_wd_insym_go2menu_3_Example.html`. Source: `c_wd_insym_go2menu.html`.
- `c:wd_insym_gomenu` - Jump to and display the "mnum" submenu page in the current icon menu file. A pick on this menu will feed the selected button's value to the INS COMPONENT command. Signature: `(c:wd_insym_gomenu mnum)`. Parameters: `mnum` = Argument mnum is passed seperately. Menu page number to display (i.e. the "**Mx" numbers in the wd_menu.dat file, or whatever menu is current).. Example topic: `_c_wd_insym_gomenu_3_Example.html`. Source: `c_wd_insym_gomenu.html`.
- `c:wd_insym_same` - User hook into the INS COMPONENT command, just-like component, insert prompted. Same as wd_insym but user prompted to select a "just like" component (schematic or panel footprint). Then user is prompted for insertion point. INS/EDIT dialog box appears after insert. The icon menu that pops up is the schematic icon menu that is in memory or is the default schematic icon menu (if the menu isn't already in memory. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insym_same)`. Returns: Ent name of inserted symbol or of deleted symbol if cancel out of the EDIT dialog box. Returns nil if symbol not found (AcadE searches in its normal list of paths for the symbol). Source: `c_wd_insym_same.html`.
- `c:wd_insym2` - User hook into the INS COMPONENT command. All data is supplied on the command line with optional EDIT dialog box popping up after symbol inserts. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insym2 sym xy scl options)`. Parameters: `sym` = Symbol block name (AcadE's search path is used when full path name is omitted). Block name is auto-adjusted, if possible, to align with an underlying wire.; `xy` = xy insertion point coordinate, nil=user prompted for insertion point.; `scl` = scale (nil=1.0); `options` = 1's bit set = pop open "INSERT/EDIT" dialog box after insertion.2's bit set = return "handle" of inserted symbol (otherwise return entity name).4's bit set = (and when 1's bit = 0) suppress parent component or terminal tag auto-generation.8's bit set = allow motor angled connections to go to non-wire LINE entity (used by CIRC BLDR).. Returns: ent name or "handle" (see "options") of inserted symbol or nil if fail. Example topic: `_c_wd_insym2_3_Example.html`. Source: `c_wd_insym2.html`.
- `c:wd_insymn` - User hook into the INS COMPONENT command, name via command-line, insert prompted. Same as wd_insym but component name supplied on command line, user prompted to select insertion point. INS/EDIT dialog box displays after insert.The icon menu that pops up is the schematic icon menu that is in memory or is the default schematic icon menu (if the menu isn't already in memory. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insymn sym)`. Parameters: `sym` = Symbol block name (AcadE's search path is used when full path name is omitted). Block name is auto-adjusted, if possible, to align with an underlying wire.. Returns: Ent name of inserted symbol or of deleted symbol if cancel out of the EDIT dialog box. Returns nil if symbol not found (AcadE searches in its normal list of paths for the symbol). Example topic: `_c_wd_insymn_3_Example.html`. Source: `c_wd_insymn.html`.
- `c:wd_insymn_repeat` - User hook into the INS COMPONENT command, name via command-line, insert prompted, insertion of sym repeated. Keeps repeating the insert of selected "sym". Component name supplied on command line, user prompted to select insertion point and will attempt to auto-align with any underlying wire. The Insert/Edit dialog box appears after each insert. The icon menu that pops up is the schematic icon menu that is in memory or is the default schematic icon menu (if the menu isn't already in memory. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insymn_repeat sym)`. Parameters: `sym` = Symbol block name (AcadE's search path is used when full path name is omitted). Block name is auto-adjusted, if possible, to align with an underlying wire.. Returns: nil. Source: `c_wd_insymn_repeat.html`.
- `c:wd_insymn_rot` - User hook into the INS COMPONENT command, name via command-line, insert prompted, rotation prompted. Component name supplied on command line, user prompted to select insertion point, but if the device cannot find a wire/line connection to tie to, it then prompts the user for an insertion ROTATION value. The INS/EDIT dialog box opens after insert. The icon menu that pops up is the schematic icon menu that is in memory or is the default schematic icon menu (if the menu isn't already in memory. Component automatically breaks any underlying wire(s), new wire number generates on new wire segment (if required), component tag automatically generates based on inserted line ref or next available sequential. Signature: `(c:wd_insymn_rot sym)`. Parameters: `sym` = Symbol block name (AcadE's search path is used when full path name is omitted). Block name is auto-adjusted, if possible, to align with an underlying wire.. Returns: Ent name of inserted symbol or of deleted symbol if cancel out of the EDIT dialog box. Returns nil if symbol not found. Source: `c_wd_insymn_rot.html`.
- `c:wd_loadmenu` - Activate AcadE icon menu. Make AcadE icon menu file "fnam" the active icon menu (for INSERT COMPONENT using c:wd_insym_dlg, c:wd_insym_f, and others). Signature: `(c:wd_loadmenu fnam)`. Parameters: `fnam` = Icon menu ascii data file (= nil defaults to "wd_menu.dat").. Source: `c_wd_loadmenu.html`.
- `c:wd_menu_load_schem` - Loads the default schematic icon menu .dat file is loaded and "in memory" so that it will display for a subsequent call related to INS COMPONENT. The menu that is loaded is determined in this order: 1st - an override menu name is defined in global GBL_wd_sch_menu_override. 2nd - defined for the active project (right -click on project's "Symbol Libraries"). 3rd - defaults to menu file "ace_jic_menu.dat". This routine can be used in ".cui" file calls to "wd_insym_gomenu mnum" so that AcadE will display schematic icon menu pages and not menu pages from the last icon menu file used (if it wasn't a schematic icon menu file). Signature: `(c:wd_menu_load_schem)`. Example topic: `_c_wd_menu_load_schem_3_Example.html`. Source: `c_wd_menu_load_schem.html`.
- `c:wd_movesym2` - User hook into the MOVE COMPONENT command, all data supplied on the command line. Moved component automatically aligns with and breaks underlying wire(s), new wire number generates on new wire segment (if required), component tag can automatically generate based on inserted line ref or next available sequential. Wire "heals" where component moved from. Signature: `(c:wd_movesym2 ben xy options)`. Parameters: `ben` = Ent name of block insert to move. nil = user prompted; `xy` = xy "move to" insertion point coordinate (nil=user prompted); `options` = 1's bit set = suppress "re-tag" check.2's bit set = return "handle" of inserted symbol.(not set = ent name)nil = open Component(s) Moved dialog box prompting user for retag or no retag. Returns: Handle of newly moved component Example topic: `_c_wd_movesym2_3_Example.html`. Source: `c_wd_movesym2.html`.
- `c:wd_pinlist_attach` - Populate parent component with PINLIST info based upon its mfg/cat combo carried on the symbol. If a match is found in the _PINLIST table of the catalog lookup file then that data is written to the symbol's PINLIST attrib (or xdata). Signature: `(c:wd_pinlist_attach ben options)`. Parameters: `ben` = Ent name of schematic block insert to process.; `options` = 1's bit set = update "coil" pin (TERMxx) attributes on the symbol.. Source: `c_wd_pinlist_attach.html`.
- `c:wd_retag_update_noprompts` - Re-tag schematic component(s) on current dwg only and optionally updates related child components across active project. Signature: `(c:wd_retag_update_noprompts options ent_lst)`. Parameters: `options` = 0 = user prompt to select components to retag, user prompted for OK to check other dwgs in project for related children (unless bit 32 set).1's bit to do dwg-wide retag update without prompting.2's also do project wide retag update without prompting.4's bit set = DESCx was changed on parent and nothing else.8's bit set = DESCx changed on parent along with TAG, CAT, or something else significant.16's bit set = processing/updating CHILD cross-refs only (no retag).32's bit set = (with 1's bit set) - suppress child check on other dwgs of project.64's bit set = (with 1's bet set) - suppress project child "Now/Later" dialog box, do "Now".; `ent_lst` = (with 1's bit set) nil = do all components on dwg, OR else do ents passed in this argument. Can be single entity name, entity handle, list of entity names, list of entity handles, or selection set.. Source: `c_wd_retag_update_noprompts.html`.
- `c:wd_xref_doit` - Run project-wide parent/child cross-reference text update. Note: the drawing or drawings to be processed must be part of the active project. Signature: `(c:wd_xref_doit dlst child_update no_report_dlg)`. Parameters: `dlst` = List of dwgs to process (full file names, double back slashes) nil = process all dwgs in current project set.; `child_update` = 1's bit set to do DESC update.2's bit set to do just blank child DESC update.4's bit set to do child LOC update.; `no_report_dlg` = 1 to suppress cross-ref exception report dlg. nil = show report. Example topic: `_c_wd_xref_doit_3_Example.html`. Source: `c_wd_xref_doit.html`.

### Section B - Panel Components

Insert Panel layout footprint and annotate.

- Source topic: `Section_B_-_Panel_Components.html`
- Routine count: 16

- `c:ace_get_footprint_fnam` - Query footprint_lookup.mdb for footprint file name. Signature: `(c:ace_get_footprint_fnam param_lst)`. Parameters: `param_lst` = (list mfg cat assycode flag) (nth 0) = MFG(nth 1) = CAT(nth 2) = ASSYCODE (nil or "" = none)(nth 3) = "ins_mode" flag: "0" = use normal lookup tables (mech footprints), "1" = use wiring diagram tables (+"_WD" table names), "2" = look for AcadE table first, then normal mech table if no match found, "16" = = non-quiet mode, display file search message in command window.. Returns: (list recval fullblknam) where "recval" = block name pulled from footprint_lookup.mdb file, "fullblknam" = full path/block name (if found). If the first character of recval is an asterisk (*), then this flags that the returned block is marked to be exploded upon insert. Source: `c_ace_get_footprint_fnam.html`.
- `c:ace_ins_dinrail` - Insert parametrically build DIN rail, no dialog box, no prompts Signature: `(c:ace_ins_dinrail param_lst)`. Parameters: `param_lst` = ("mfg", "cat", "asmb" = catalog manufacturer, catalog, assycode (if required)) (nth 0) : xyz - insertion point(nth 1) : len - length(nth 2) : horv - horiz or vertical insert "H" or "V"(nth 3) : scale, nil = 1.0(nth 4) : pnl_mounting 1 = holes, 2 = stand-offs, 3 = none(nth 5) : options (1's bit set = show the EDIT dialog box after insertion). Example topic: `_c_ace_ins_dinrail_3_Example.html`. Source: `c_ace_ins_dinrail.html`.
- `c:wd_adjust_pnllays` - Move attributes found on panel footprint block insert "ben" to the appropriate layer categories defined on the drawing's WD_PNLM block insert. Signature: `(c:wd_adjust_pnllays ben)`. Parameters: `ben` = AutoCAD entity name of footprint block.. Returns: List of changes made, or nil if no change. Format: Example topic: `_c_wd_adjust_pnllays_3_Example.html`. Source: `c_wd_adjust_pnllays.html`.
- `c:wd_dinrl` - Insert parametrically built DIN rail. User prompted for insertion point, length, direction, and so on. Signature: `(c:wd_dinrl)`. Source: `c_wd_dinrl.html`.
- `c:wd_do_psym_in` - Triggers panel footprint icon menu display for component selection. User selects insertion point and rotation. Signature: `(c:wd_do_psym_in)`. Source: `c_wd_do_psym_in.html`.
- `c:wd_edsym2` - Trigger EDIT component dialog box for panel footprint entity "ben". The component can also be a PLC module, schematic symbol, a wire signal source or destination arrow, or footprint. AcadE pops open the appropriate dialog box based upon selected component type. Signature: `(c:wd_edsym2 ben)`. Parameters: `ben` = AutoCAD entity name of selected AcadE component.. Example topic: `_c_wd_edsym2_3_Example.html`. Source: `c_wd_edsym2.html`.
- `c:wd_ins_np` - User entry point for inserting a smart nameplate. Signature: `(c:wd_ins_np fpben npblk xy rot scl xgrp noprompts anno_dat)`. Parameters: `fpben` = Footprint block ent name to tie nameplate to (nil = insert as stand-alone nameplate).; `npblk` = Nameplate block name (nil = figure out from mfg/cat/asmb lookup).; `xy` = Nameplate insertion point (nil = prompt user for pick point if fpben = nil and noprompts = nil). If fpben given, inserts at its origin.; `rot` = rotation angle, nil = prompt user if noprompts = nil, nil = 0.0 if noprompts = 1.; `scl` = scale, nil=insert at current symbol setting scale, or optional x and y scale passed as a list: (list xscl yscl).; `mfg` = manufacturer code assignment (optional); `cat` = catalog code assignment (optional); `asmb` = assemble code assignment (optional); `xgrp` = 1 to explode and GROUP (For example, a nameplate block nested inside a stretchable border. Nil = insert as normal unexploded block.; `noprompts` = 1 to suppress all user prompts and no edit dialog box and also return quietly from the routine if any needed data is missing or invalid.; `anno_dat` = annotation data list (any entry can have a value or be set to nil or "").. Returns: nil if failed to run, otherwise return ent name list of lists in this order: (list footprints nameplates balloons term_footprints). Example topic: `_c_wd_ins_np_3_Example.html`. Source: `c_wd_ins_np.html`.
- `c:wd_insymp` - Insert panel footprint block. Footprint can be totally "dumb" with no prepositioned AcadE attributes (ex: footprints straight from a vendor) or AcadE-aware with attributes. If no attributes, AcadE adds annotation data to the block on -the-fly in the form of AutoCAD extended entity data (XDATA). Signature: `(c:wd_insymp fpblk xy rot scl anno_dat)`. Parameters: `fpblk` = Panel footprint block to insert (searches normal AcadE paths).; `xy` = Insertion point (nil = user prompted).; `rot` = Rotation (in degrees), nil = 0.0 degrees.; `scl` = scale (default = 1.0), optional (list xscl yscl) allowed.; `anno_dat` = future/optional list of annotation data, pass "nil" for now.. Returns: Entity name of new block insert. Source: `c_wd_insymp.html`.
- `c:wd_insymp_r` - Simple footprint insert, with rotate prompt. Signature: `(c:wd_insymp_r footprint)`. Parameters: `footprint` = footprint block/file name to insert.. Returns: nil if failure or ent name of inserted block insert. Example topic: `_c_wd_insymp_r_3_Example.html`. Source: `c_wd_insymp_r.html`.
- `c:wd_is_it_pnl` - Check entity type. Determine if given entity is a panel footprint, panel terminal, nameplate symbol, or item balloon. Signature: `(c:wd_is_it_pnl ben)`. Parameters: `ben` = name of entity to query.. Returns: nil = not panel symbol, "FP" = footprint, "FPT" = panel terminal, "NP" = panel nameplate, "BAL" = item balloon. Example topic: `_c_wd_is_it_pnl_3_Example.html`. Source: `c_wd_is_it_pnl.html`.
- `c:wd_pnl_asmb2` - For inserting assy of one or more panel footprints, terminal footprints, nameplates, and/or balloons. Signature: `(c:wd_pnl_asmb2 symname xy scl rot flgbits)`. Parameters: `symname` = wblocked dwg to insert exploded (nil=user prompted to choose).; `xy` = Insertion point (nil=user prompt).; `scl` = Scale (nil=GBL_wd_symscl value if exist or 1.0 if not).; `rot` = Rotation angle in degrees (nil=prompt user).; `flgbits` = 1's bit set = convert any dumb blocks found in the exploded assembly to "panel" footprints.1's bit AND 2's bit set = add in attribute template for converted footprints.Set flgbits = nil or 0 to leave any dumb blocks as dumb blocks.. Returns: nil if failed to run, otherwise return ent name list of lists in this order: (list footprints nameplates balloons term_footprints). Source: `c_wd_pnl_asmb2.html`.
- `c:wd_pnl_ed` - Obsolete. Use wd_psymed instead. Signature: `(c:wd_pnl_ed)`. Source: `c_wd_pnl_ed.html`.
- `c:wd_pnl_lout` - Find and insert footprint. Select component from AcadE Schematic extract list (.wd1 file), find and insert matching footprint (based upon schematic component's MFG and catalog number, then start the EDIT footprint dialog box. Signature: `(c:wd_pnl_lout fnam)`. Parameters: `fnam` = Panel extract csv file ("nil" = access project's database file).. Source: `c_wd_pnl_lout.html`.
- `c:wd_pnlb_doedit` - This function has been renamed, use wd_psymed instead. Signature: `(c:wd_pnlb_doedit)`. Source: `c_wd_pnlb_doedit.html`.
- `c:wd_psymed` - Select a panel footprint to edit. Prompts user to select a panel footprint to edit, then starts EDIT footprint dialog box. Signature: `(c:wd_psymed)`. Source: `c_wd_psymed.html`.
- `c:wd_ptrm_lout` - Insert panel terminal footprint symbols. Symbols picked from schematic terminal extract list. Signature: `(c:wd_ptrm_lout fnam)`. Parameters: `fnam` = panel extract csv file ("nil" = access project's database file).. Source: `c_wd_ptrm_lout.html`.

### Section C - Wires and Wire Numbers

Insert wires and wire numbers.

- Source topic: `Section_C_-_Wires_and_Wire_Numbers.html`
- Routine count: 33

- `ace_get_default_wiretype` - Get the default wire layer on the active drawing where the wires will be inserted when the "Move all lines to wire layers" is checked. Signature: `(ace_get_default_wiretype)`. Source: `ace_get_default_wiretype.html`.
- `ace_remove_wiretype` - Remove the specify layer from wire type on the active drawing. The layer is not deleted from the drawing. Signature: `(ace_remove_wiretype layername)`. Parameters: `layername` = The name of the layer. Source: `ace_remove_wiretype.html`.
- `ace_set_default_wiretype` - Set the default wire type on the active drawing which can be used to set the wire layer when inserting a saved circuit. Signature: `(ace_set_default_wiretype layername)`. Parameters: `layername` = The name of the layer to be set. Source: `ace_set_default_wiretype.html`.
- `ace_wnum_find` - Test whether the wire number has been used. If the wire number in use, then return the next unused wire number. Signature: `(ace_wnum_find paramlst)`. Parameters: `paramlst` = wire number (text string) to test, OR (list option start fmt w_layer dwg_wirenum_lst proj_wirenum_lst) where:"options_lst" = wire number (text string) to test, OR(list option start fmt w_layer dwg_wirenum_lst proj_wirenum_lst)"option" = future"base_start" = start search at this base wire number (the %N base part of the wire number)(nil = WD_M block's WIRE-START attrib value)"fmt" = wire number format (nil = format is just "%N")"w_layer" = optional wire layer name (for wire numbering on per-layer format)"dwg_wirenum_lst" = optional list of existing wire nums on active drawing (speed-up). Example topic: `_ace_wnum_find_3_Example.html`. Source: `ace_wnum_find.html`.
- `c:ace_del_wnum` - Delete wire number and any extra wire number copies found on target wire network. This routine also blanks out the connected X?TERMxx attribute values for component connections tied to the wire network. If the wire number is an "in-line" wire number, the wire will "heal" after the wire number is removed. Signature: `(c:ace_del_wnum en)`. Parameters: `en` = Entity name of LINE wire of network or of wire number block insert.. Returns: Failure returns nil. Success returns (list wlst) where "wlst" is a list of wire LINE entity segments of the processed wire network. Example topic: `_c_ace_del_wnum_3_Example.html`. Source: `c_ace_del_wnum.html`.
- `c:ace_fix_wnum` - Change existing wire number to either FIXED or NORMAL. Signature: `(c:ace_fix_wnum en options)`. Parameters: `en` = entity name of LINE wire or of wire number block INSERT.; `options` = nil or 1 to change to FIXED, 0 to change to NORMAL.. Returns: Failure returns nil. Success returns 1 (even if nothing changed). Source: `c_ace_fix_wnum.html`.
- `c:ace_get_wiretype_data` - Get the full list of data associated with wiretype "layername" on drawing in drawing "fnam" of the active project. Signature: `(c:ace_get_wiretype_data  layername  fnam)`. Parameters: `layername` = the wire type; `fnam` = drawing path/file name, drawing name without path, drawing name without ".dwg", or nil=active drawing. Returns: nil if not found or wiretype list of data in this format: (list color size USER1 USER2 ... USER20 wirenumbering) where "wirenumbering" = "1" if "Yes" and "0" if "No" Source: `c_ace_get_wiretype_data.html`.
- `c:ace_get_wiretype_list` - Get list of defined wire types (i.e. wire "layers") from target drawing file (or active drawing) Signature: `(c:ace_get_wiretype_list param_lst)`. Parameters: `param_lst` = "param_lst" = nil for active drawing or (list dwg_filename). Use double backslashes or single forward slashes for passed file name.. Returns: nil if none found or list of defined wire type names like this: ("BLK_14AWG" "WHT_14AWG" "RED_18AWG") Source: `c_ace_get_wiretype_list.html`.
- `c:ace_get_wnum` - Retrieve wire number value from selected wire network. Signature: `(c:ace_get_wnum wen)`. Parameters: `wen` = Entity name of LINE wire segment of network.. Returns: nil on failure, else (list wnum_txt wnum_blk_en) where "wnum_txt" = wire number text (WIRENO or WIRENOF attrib value) and "wnum_blk_en" = ent name of wire number block insert. Example topic: `_c_ace_get_wnum_3_Example.html`. Source: `c_ace_get_wnum.html`.
- `c:ace_insert_wire` - Insert wire routing, with auto-routing and collision checking option. If wire crosses another wire, AcadE automatically adds a gap or loop at crossing point (if defined in dwg's CONFIG dialog box). If it crosses one or more devices floating in space, it will break across the components if wire connection points are in the wire's path. Signature: `(c:ace_insert_wire param_lst)`. Parameters: `param_lst` = Flags: nth 0 = starting XY coordinate (can be empty space, from existing LINE wire, or at a component's wire connection point.nth 1 = ending XY coordinate.nth 2 = "layer" = nil to default to current wire layer or, if current layer not a wire layer then default to first wire layer defined. Otherwise, insert new wire on layer "layer" (automatically created if it doesn't exist).nth 3 = "options"=1's bit set = start and end wires at exact "From" and "To" locs. Don't adjust ends to mate with some wire or component connection point.=2's bit set = Do not filter out short line segments.=4's bit set = "auto path calculation" mode is ON.=8's bit set = suppress returning list of inserted wire LINE handles.. Returns: list of inserted wire LINE handles (or nil if options bit 8 is set). Example topic: `_c_ace_insert_wire_3_Example.html`. Source: `c_ace_insert_wire.html`.
- `c:ace_mod_wiretype` - Modify existing wire type parameter value assignments. Signature: `(c:ace_mod_wiretype layer_name layer_data options_lst)`. Parameters: `layer_name` = Layer name to modify (must be existing wire layer); `layer_data` = List of parameter and value pairs, ex: (list "COLOR" "RED" "SIZE" "14" "USER1" "THHN") where the first entry in the list is the layer name itself. Note: the COLOR parameter is a data value for the wire type, it is not used to set the layer's default color value.; `options_lst` = nil (not used). Returns: nil if fail or wire layer name if success. Example topic: `_c_ace_mod_wiretype_3_Example.html`. Source: `c_ace_mod_wiretype.html`.
- `c:ace_new_wiretype` - Add new wire layer to active drawing. Signature: `(c:ace_new_wiretype layer_name layer_data options_lst)`. Parameters: `layer_name` = Layer name to create.; `layer_data` = List of parameter and value pairs, for example: (list "WIRE COLOR" "RED" "SIZE" "14" "USER1" "THHN").where the first entry in the list is the layer name itself.Note: the COLOR parameter is a data value for the wire type, it is not used to set the layerâ€™s default color value.Use "WIRENO" flag with value of "0" to flag the wiretype as "no wire numbering".; `options_lst` = nil (not used). Returns: nil if failure, or new wire layer name if success. Example topic: `_c_ace_new_wiretype_3_Example.html`. Source: `c_ace_new_wiretype.html`.
- `c:ace_sig_update` - Update cross-referencing on Wire Signals or Stand-Alone reference symbols. Signature: `(c:ace_sig_update param_lst)`. Parameters: `param_lst` = (list dowhat dowir doref doshow entlst options) where:dowhat = 1 to process wire signals, = 0 to process stand-alone cross-ref symbolsdowir = (if dowhat=1) 1 to process wire nums tied to signals; 0=skipdoref = 1 to update cross-refs on signals; 0= skipdoshow = 1 to show src/dst links with temp graphics, 0=skipentlst = list of entities to process; = nil to process entire dwgoptions = future. Source: `c_ace_sig_update.html`.
- `c:ace_toggle_inline` - Toggle an in-line wire number to an "above/below" line wire number or vice-versa. Signature: `(c:ace_toggle_inline en options)`. Parameters: `en` = Entity name of LINE wire or the wire number block INSERT.; `options` = For future use.. Returns: Nil if no action, otherwise returns handle of the toggled wire number block insert. Source: `c_ace_toggle_inline.html`.
- `c:ace_wnum_find_wire_en` - Find entity name of LINE wire underneath given wire number block insert. Signature: `(c:ace_wnum_find_wire_en wnum_en trap)`. Parameters: `wnum_en` = Wire number block insert.; `trap` = trap distance to search outward from wire number block insert's XY insertion point. If nil then default to drawing's default trap distance given in global GBL_wd_trp.. Returns: Total failure returns nil. Success or partial success returns (list wire_en line_lst) - where: "wire_en" = entity name of valid LINE wire found under the wire number block insert and with a pointer pointing back at the wire number block insert. Failure returns nil for wire_en. "line_lst" = list of all LINE entities found under the wire number insert (success if only one and it is the same as "wire_en"). Source: `c_ace_wnum_find_wire_en.html`.
- `c:wd_do_str_wire` - Stretch wire to try to connect to a wire connection point. Signature: `(c:wd_do_str_wire wire_en loc)`. Parameters: `wire_en` = Entity name of WIRE line segment.; `loc` = Identify which end of LINE to stretch.. Source: `c_wd_do_str_wire.html`.
- `c:wd_get_sym_pntlst` - Extract list of lists of wire connection point information. Get connection point information from component entity "ben". Signature: `(c:wd_get_sym_pntlst ben nozoom usecache)`. Parameters: `ben` = Entity name of component (block insert with attributes).; `nozoom` = 1 to suppress zoom ext, otherwise allow zoom ext if target symbol not entirely on screen.; `usecache` = 1 to look for target "ben" in existing cache.. Returns: List of lists, one list for each wire connection point carried on the target entity. For example: (list (list index connection_xyz nil conn_wire_en_lst suffix xterm_ent_name multi_wire_lst maxcon jumpers_attr_en) (list index....)) Where : index = index number (integer). connection_xyz = XYZ coordinate of the X?TERMxx wire connection attribute. nil = for future use. conn_wire_en_lst = list of entity names of LINE segments that have end points that touch or "connect to" the wire connection point. xdir = the "?" character of the X?TERMxx wire connection attribute (valid values = 0, 1,2,4, or 8). suffix = the "xx" suffix characters of the X?TERMxx wire connection attribute (ex: "01"). xterm_en_name = entity name of the X?TERMxx wire connection attribute. multi_wire_lst = list of entity names of polylines on the "_MULTI_WIRE" layer that tie into the wire connection point. maxcon = (future) value of "PREFER_MAXCON" xdata carried on wire connection attribute. jumpers_attr_en = (may not be present in the list) - entity name of WD_JUMPERS attribute (if exists, otherwise this entry is omitted from the list). Source: `c_wd_get_sym_pntlst.html`.
- `c:wd_get_wire_netlst` - Extract data related to wire netlist. Netlist includes wire segment entity "wen". Signature: `(c:wd_get_wire_netlst wen options)`. Parameters: `wen` = AutoCAD entity name of line wire segment.; `options` = nil = no options.Bit 1 = 0 to ignore all terminal symbols that don't change wire num and read through symbol screw terms when mult wire connection points on screw term.Bit 1 = 1 (for various wire connection reports). All terminals treated same as components. Break wire and don't read through a symbol's screw term with mult.Bit 2 = 1 suppress extract of component and X?TERM attributes.Bit 4 = 1 to use GBL_wd_ecache and GBL_wd_dcache "pntlst" cache.Bit 8 = 1 to not read through CABLE (?W0*") markers.Bit 16 = single-step debug trace mode.Bit 32 = 1 for quick CABLE marker search on network.Bit 64 = 1 to also process LINE entities on "multi-wire" line layer.Bit 128 = 1 to force PNLWDLEV query or search thru ents on current dwg for WDLEV values.. Returns: List of lists: (list wlst en1st xmin ymax s_sig d_sig trmlst xtermlst cmplst cmpconlst fan_inout_lst nil) where: wlst = list of wire LINE segment entity names that make up the network. en1st = entity name of upper left-most "beginning" wire segment of the network. xmin = ymax = s_sig = d_sig = trmlst = xtermlst = cmplst = cmpconlst = list of connected component connection data lists (list (list comp_en xterm_en wire_en cbl_en wireseqval future prefer_maxcon) ...) where: comp_en = entity name of connected component. xterm_en = entity name of "comp_en" wire connection attribute tied to network. wire_en = entity name of LINE wire tied to "xterm_en". wireseqval = optional wire sequence code assigned to "xterm_en", default="900". prefer_maxcon = optional max wire connection value tied to "xterm_en". fan_inout_lst = list of any fan in/out symbols. Example topic: `_c_wd_get_wire_netlst_3_Example.html`. Source: `c_wd_get_wire_netlst.html`.
- `c:wd_inwire_connect_doit` - Inserts wiring between components if they have wire connection points that are "in line" with each other. Signature: `(c:wd_inwire_connect_doit ben1 ben2 layer)`. Parameters: `ben1` = AutoCAD entity name (or handle) of first component.; `ben2` = AutoCAD entity name (or handle) of second component.; `layer` = nil to default to current wire layer or, if current layer not a wire layer then default to first wire layer defined. Otherwise, insert new wire on layer "layer".. Source: `c_wd_inwire_connect_doit.html`.
- `c:wd_move_wn` - Move existing wire number entity to a new "XY" coordinate on a line entity "line_en" of the same wire network. If the new location does not provide enough room, the wire number tries to position itself on a leader. Signature: `(c:wd_move_wn old_ben line_en xy)`. Parameters: `old_ben` = Wire number block name.; `line_en` = Ent name of wire segment to move wire number to.; `xy` = xy coordinate on the line where wire number to move to.. Source: `c_wd_move_wn.html`.
- `c:wd_putwn` - Insert normal (not "fixed") wire number "newval" on wire entity "en". Inserted above, below, or in-line based upon the drawing's configuration. AcadE automatically positions the wire number on or in wire segement "en", adding a leader, if necessary, when not in-line and in a congested area. If an existing wire number is found on the segment or anywhere on the network that wire segment "en" is a part of, AcadE updates the existing wire number with value "newval". Signature: `(c:wd_putwn en newval)`. Parameters: `en` = AutoCAD entity name of LINE wire.; `newval` = new wire number value.. Returns: Ent name of the wire number block insert. Example topic: `_c_wd_putwn_3_Example.html`. Source: `c_wd_putwn.html`.
- `c:wd_putwnf` - Same as wd_putwn but insert fixed wire number. Signature: `(c:wd_putwnf en newval)`. Parameters: `en` = AutoCAD entity name of LINE wire.; `newval` = new wire number value.. Example topic: `_c_wd_putwnf_3_Example.html`. Source: `c_wd_putwnf.html`.
- `c:wd_putwnxy` - Same as wd_putwnxyf but inserts a regular wire number. Signature: `(c:wd_putwnxy xy newval)`. Parameters: `xy` = Point that the line passes through.; `newval` = New wire number value.. Source: `c_wd_putwnxy.html`.
- `c:wd_putwnxyf` - Put fixed wire number "newval" on LINE that passes through coordinate "xy". AcadE automatically positions the wire number on the wire segment/network passing through location "xy". If an existing wire number is found on the segment or anywhere on the network that wire segment "en" is a part of, AcadE updates the existing wire number with value "newval". Signature: `(c:wd_putwnxyf xy newval)`. Parameters: `xy` = Point that the line passes through.; `newval` = New wire number value.. Returns: Ent name of the wire number block insert. Source: `c_wd_putwnxyf.html`.
- `c:wd_putwnxyfq` - Same as wd_putwnxyf but no error checking and assumes NO existing wire number on network (runs faster in automated drawing generation programs). Signature: `(c:wd_putwnxyfq xy newval)`. Parameters: `xy` = Point that the line passes through.; `newval` = New wire number value.. Source: `c_wd_putwnxyfq.html`.
- `c:wd_putwnxyq` - Same as wd_putwnxy but no error checking and assumes NO existing wire number on network (runs faster in automated drawing generation programs). Signature: `(c:wd_putwnxyq xy newval)`. Parameters: `xy` = Point that the line passes through.; `newval` = New wire number value.. Source: `c_wd_putwnxyq.html`.
- `c:wd_strblk_inline_wn` - Adjust gap spacing of in-line wire number or in-line wire label. Signature: `(c:wd_strblk_inline_wn en param_lst)`. Parameters: `en` = Entity name of the in-line wire number or in-line wire label.; `param_lst` = (list min_width text_to_line_gap snapx) where: "min_width" = 0.0 to minimize, <0.0 minimum total gap width"text_to_line_gap" = minimum gap between edge of text and start of wire line"snapx" = snap increments for gap width change, 0.0 or nil = continuous adjustment.. Example topic: `_c_wd_strblk_inline_wn_3_Example.html`. Source: `c_wd_strblk_inline_wn.html`.
- `c:wd_trimwire` - Trim wire "ent" at loc "xy". Remove wire connection dot(s) as required. Signature: `(c:wd_trimwire ent xy)`. Parameters: `ent` = Entity name of wire to trim.; `xy` = Location for trim point.. Source: `c_wd_trimwire.html`.
- `c:wd_ungap` - Attempt to remove any unrequired gap/loops from wire segment. Signature: `(c:wd_ungap wen)`. Parameters: `wen` = AutoCAD entity name of wire segment to process.. Returns: nil if no gaps removed, otherwise returns handle of the modified wire segment. Source: `c_wd_ungap.html`.
- `c:wd_wire` - Insert single wire segment. If wire crosses another wire, AcadE automatically adds a gap or loop at crossing point (if defined in dwg's CONFIG dialog box). If it crosses one or more devices floating in space, it will break across the components if wire connection points are in the wire's path. Signature: `(c:wd_wire end1 end2 layer)`. Parameters: `end1` = Start coordinates.; `end2` = End coordinates.; `layer` = nil to default to current wire layer or, if current layer not a wire layer then default to first wire layer defined. Otherwise, insert new wire on layer "layer" (layer automatically created if it doesn't exist).. Example topic: `_c_wd_wire_3_Example.html`. Source: `c_wd_wire.html`.
- `c:wd_wire_num_qm` - Change wire number attributes to "?" (this is used prior to a project-wide sequential wire numbering operation). Signature: `(c:wd_wire_num_qm)`. Source: `c_wd_wire_num_qm.html`.
- `c:wd_wire_quik` - Insert wire segment, quick process. Same as wd_wire but with reduced error checking (runs faster in automated drawing generation programs). If wire crosses another wire, AcadE automatically adds a gap or loop at crossing point (if defined in dwg's CONFIG dialog box). If it crosses one or more devices floating in space, it will break across the components if wire connection points are in the wire's path. Signature: `(c:wd_wire_quik end1 end2 layer)`. Parameters: `end1` = Start coordinates.; `end2` = End coordinates.; `layer` = nil to default to current wire layer or, if current layer not a wire layer then default to first wire layer defined. Otherwise, insert new wire on layer "layer" (layer automatically created if it doesn't exist).. Source: `c_wd_wire_quik.html`.
- `c:wd_wnums_doit` - Project-wide wire number tagging batch process. Signature: `(c:wd_wnums_doit dlist freshen param_lst)`. Parameters: `dlist` = List of dwgs to process (full file names, double back slashes) (nil = process all dwgs in active project set).; `freshen` = "1" to force database freshen, "0" = no freshen.; `param_lst` = (nth 0) = "0"(nth 1) = "1" to recalc/overwrite old wire nums, "0" = process, untagged wires only (skip existing wire numbers).(nth 2) = starting wire num text string (sequential mode only).(nth 3) = wire number format (override value).(nth 4) = "1" to trigger SIGNAL arrow cross-ref processing, "0" to not do SIGNAL cross-referencing update.(nth 5) = "C" = consecutive sequential from one drawing to the next (sequential mode only), "S" = use each dwgs CONFIG "starting wire num" (for sequential mode only), "R" = reference based tagging mode.(nth 6) = "1" to run prelim pass to erase all existing wire nums, "0" to not make prelim pass to erase existing wire nums.(nth 7) = ""(nth 8) = nil or "0" for Normal wire numbers, "1" for Fixed wire numbers.. Example topic: `_c_wd_wnums_doit_3_Example.html`. Source: `c_wd_wnums_doit.html`.

### Section D - PLC I/O

PLC functions.

- Source topic: `Section_D_-_PLC_I_O.html`
- Routine count: 6

- `c:wd_find_sel_plc` - Returns list of PLC database "data" to be used by the (c:wd_inplc_nd...­) routine to insert a PLC module. Signature: `(c:wd_find_sel_plc catkey)`. Parameters: `catkey` = PLC module "key" value to check for match in PLC*.DAT file.. Example topic: `_c_wd_find_sel_plc_3_Example.html`. Source: `c_wd_find_sel_plc.html`.
- `c:wd_inplc` - Invoke PLC selection dialog box. Pick and insert PLC module per user prompts. Signature: `(c:wd_inplc)`. Source: `c_wd_inplc.html`.
- `c:wd_inplc_altdat` - Invoke PLC selection dialog box, supply database. Use "datfnam" instead of defaulting to "ace_plc.mdb" for manufacturer file selection. Signature: `(c:wd_inplc_altdat datfnam)`. Parameters: `datfnam` = List of vendor names/parametric database.. Source: `c_wd_inplc_altdat.html`.
- `c:wd_inplc_nd` - Insert PLC module. Starts at "plc_xy". Allows partial module to be inserted (i.e. splitting long module into multiple inserts). Signature: `(c:wd_inplc_nd plc_xy style firstaddr veryfirsta base_carry beg_ix end_ix usr_rv orientation data alst)`. Parameters: `plc_xy` = Insertion point.; `style` = PLC "style" number ("1" through "9"). Defines the appearance of the graphics that will be generated.; `firstaddr` = First addr for module or this piece of broken module (or list of starting addresses for "combo" module).; `veryfirsta` = First addr of the overall module when dealing with insertion of a split piece. Nil= no split or use "firstaddr" value.; `base_carry` = Numbering method (8 = octal, 10 = decimal, 16 = hex, nil = prompt user if necessary).; `beg_ix` = 1; `end_ix` = Count of last addr point (for example, 16 or 32 or any large integer number).; `usr_rv` = Rung spacing. Nil uses the same as an existing ladder of if not ladders uses the drawing default.; `orientation` = "H" or "V" for horizontal module or vertical module (def).; `data` = List of data returned by wd_find_sel_plc.; `alst` = nil if no "prompt responses", or (list "" "%%1 prompt answer" "%%2 prompt answer" ...). Source: `c_wd_inplc_nd.html`.
- `c:wd_inplc_nd2` - Simplified PLC insert from all command line params, whole module inserts. Signature: `(c:wd_inplc_nd2 param_lst)`. Parameters: `param_lst` = (nth 0) = part number (the "K=" match in the plc*.dat files).(nth 1) = plc_xy insertion point.(nth 2) = style number "1" = style 1.(nth 3) = starting address (or list of starting addresses for "combo" module).(nth 4) = base_carry (10 = decimal, 16 = hex, 8 = octal).(nth 5) = orientation "H" = horizontal module, "V" = vertical (default).. Returns: 4 element list where (nth 3) = ent name of new PLC module inserted. Nil on failure. Source: `c_wd_inplc_nd2.html`.
- `c:wdio_autorun` - Start Spreadsheet-to-PLC I/O generation utility. Invoke AutoCAD Eletrical's Spreadsheet-to-PLC I/O generation utility from the command line. The "wdio.lsp" file must be in memory ("appload") for this to run. Signature: `(c:wdio_autorun param_lst)`. Parameters: `param_lst` = (nth 0) = xlsfnam = spreadsheet file name (.xls, .csv, .mdb).(nth 1) = wdi_fnam = settings file name (nil = use defaults).(nth 2) = continuation_dwglst (nil = increment starting dwg's file name for each new drawing required to handle the spreadsheet's data.(nth 3) = optional XLS or MDB sheet/table name (nil = use "Sheet1" or first table found with data).(nth 4) = Starting drawing file name (nil=use current active drawing â€“ must be a "named" drawing)(nth 5) = "other_options": nil=none; (1's bit set = pause to display main dialog box). Example topic: `_c_wdio_autorun_3_Example.html`. Source: `c_wdio_autorun.html`.

### Section E - Drawing Setup

Insert and edit ladders. Drawing setup. Title block.

- Source topic: `Section_E_-_Drawing_Setup.html`
- Routine count: 14

- `c:ace_find_wdt` - Find the ".wdt" title block mapping file to use for project. Signature: `(c:ace_find_wdt wdpfnam msgs)`. Parameters: `wdpfnam` = full path/filename of project ".wdp" or nil=active project; `msgs` = 1 = show error messages on the command line, nil = suppress error messages.. Example topic: `_c_ace_find_wdt_3_Example.html`. Source: `c_ace_find_wdt.html`.
- `c:ace_GBL_wd_m` - Read WD_M block on active drawing and fill and return GBL_wd_m global. Signature: `(c:ace_GBL_wd_m options)`. Parameters: `options` = 1's bit set = insert WD_M block if it does not exist without any user prompt.1's bit clear = prompt user for permission to insert WD_M block when it does not exist on the active drawing.2's bit set = if WD_M block does not exist then temporarily insert a copy, read to fill GBL_wd_m, then immediately delete the inserted instance and purge it.. Returns: Copy of global list GBL_wd_m (or nil if failure) where: nth 0 = rungs Vert or Horiz (V/H). nth 1 = Ref numbering system 4=X,Y grid; 1=lref txt with bar, 2=txt only; 3=user lref symbols used. nth 2 = TAGMODE value ("R" or "S"). nth 3 = TAG-START value (ex: "1"). nth 4 = TAG-RSUF value (ex: ",A,B,C,D,E,F"). nth 5 = TAGFMT value (ex: "%f %n"). nth 6 = WIREMODE value. nth 7 = WIRE-START string value (ex: "100"). nth 8 = WIRE-RSUF value (ex: ",A,B,C,D,E,F,G,H,J"). nth 9 = WIREFMT value (ex: "%n"). nth 10 = WINC wire number increment. nth 11 = RUNGDIST default rung spacing. nth 12 = DLADW default ladder width. nth 13 = SHEET attribute text. nth 14 = XREFFMT cross ref format (ex: "%s-%n"). nth 15 = RUNGINC rung increment (default = 1). nth 16 = DRWRUNG (= 0 don't drawing ladder rungs, 1 = draw all, 2 = every other, etc.). nth 17 = PH3SPACE 3-phase bus spacing value. nth 18,19 = DATUMX,DATUMY (X-Y grid and X-zone X and Y origin values). nth 20,21 = DISTH,DISTV (X-Y grid and X-zone increment widths). nth 22,23 = CHAR_H,CHAR_V (X-Y grid and X-zone label list). nth 24 = X-Y horiz first flag (X-Y grid format). nth 25 = X-Y delim char (X-Y grid delimiter between horiz and vertical). nth 26 = valid wire layer names, "" = all valid. nth 27,28 = valid wire num, wire num copy layers. nth 30 = units scl (ex: 1.0 or 25.4). nth 31 = feature scl (def = 1.0). nth 41 = wire leaders (1 = always, 0 = as reqd, 2 = never). nth 42 = PLC module style code (def = "1"). nth 43 = ARROW style. nth 44 = Dashed line LINK layer. nth 45 = Layer for Comp graphis. nth 46 = Sheet dwg name. nth 47 = alt xref format. nth 48 = Fixed tag layer name. nth 49 = wire gap style. nth 50 = misc flags: 1's bit = mm fullsize. nth 51-53 = IEC codes (dwg-wide). nth 54 = ref wireno layer (sig arrow wireno, terminal wireno text layer). nth 55 = misc data (future). nth 56 = valid "Fan-in/Fan-out, single-line" layers. nth 57 = Fan-in/Fan-out symbol style. nth 58 = Location Box layer. nth 59 = SORTMODE = re-tag and wire numbering sort mode. nth 60=XREF_STYLE 0=text, 1=graphical, 2=table nth 61=XREF_FLAGS= 1'sbit=include unused contacts, 2'sbit=include parent coil (table only) nth 62=XREF_UNUSEDSTYLE 0=separate reference, 1=contact count totals nth 63=XREF_FILLWITH fillwith text nth 64=XREF_SORT 0=sort by line reference, 1=sort by pinlist nth 65=XREF_TXTBTWN text format. text between separate references nth 66=XREF_GRAPHIC 0=contact mapping, 1=Graphic nth 67=XREF_GRAPHICSTYLE 0=JIC, 1=IEC nth 68=XREF_CONTACTMAP contact mapping list nth 69=XREF_TBLSTYLE table style name nth 70=XREF_TBLTITLE table title nth 71=XREF_TBLINDEX table fields to include nth 72=XREF_TBLFLDNAMS table available field names nth 73=XREF_TBLCOLJUST table fields default justification nth 74=WNUM_OFFSET drawing override for wire placement offset from end of wire, 0.0=centered nth 75=WNUM_FLAGS Source: `c_ace_GBL_wd_m.html`.
- `c:ace_ladr2_reseq_main` - Project-wide ladder reference renumber. Signature: `(c:ace_ladr2_reseq_main dist ladr_num ladr_mode ladr_skip)`. Parameters: `dist` = List of drawings to process from active project.; `ladr_num` = First ladder reference as a string, i.e. "101"; `ladr_mode` = 0 for continuous, 1 for skip (integer); `ladr_skip` = Number to skip if ladr_mode is 1. (integer). Example topic: `_c_ace_ladr2_reseq_main_3_Example.html`. Source: `c_ace_ladr2_reseq_main.html`.
- `c:wd_1_reset_dwg` - Reset AcadE drawing-specific global variables. For example, when a different drawing is made active. Signature: `(c:wd_1_reset_dwg)`. Source: `c_wd_1_reset_dwg.html`.
- `c:wd_chg_ldr_info` - Retrieve list of ladder parameters from current drawing. Signature: `(c:wd_chg_ldr_info oldlst newlst)`. Parameters: `oldlst` = List as returned by wd_get_gridrefs.; `newlst` = Edited version of "oldlst" with desired changes.. Example topic: `_c_wd_chg_ldr_info_3_Example.html`. Source: `c_wd_chg_ldr_info.html`.
- `c:wd_get_gridrefs` - Retrieve list of ladder parameters from current drawing. Signature: `(c:wd_get_gridrefs)`. Returns: List of lists of ladder parameters, one sublist for each ladder on the current drawing. Each sublist is broken down as follows: nth 0 = XY origin of ladder's first line reference. nth 1 = Ladder's first line reference text value (attribute RUNGFIRST). nth 2 = RUNGINC attribute value. nth 3 = RUNGDIST attribute value. nth 4 = RUNGCNT attribute value. nth 5 = entity name of RUNGFIRST attribute. nth 6 = Ladder's wire number format override (attribute WIREFMT). nth 7 = entity name of the first line reference block insert. nth 8 = PH3SPACE attribute value. nth 9 = RUNGHORV code ("H"=horizontal rungs, "V"=vertical rungs). nth 10 = SHEET attribute value (if present). nth 11 = entity name of SHEET attribute (if present). Source: `c_wd_get_gridrefs.html`.
- `c:wd_in_ladder` - Insert ladder. Signature: `(c:wd_in_ladder Lc_ph pt1 pt2 rungl isvert rungw rungv Lc_3sp rungi gridxy_mode Lc_dr def_1st
options)`. Parameters: `Lc_ph` = 1 for single phase ladder or 3 for 3-phase ladder.; `pt1` = xyz coordinate of left-hand, top-most reference of ladder.; `pt2` = xyz of bottom ref of ladder. If pt2 = nil then the bottom point is calculated as offset from pt1 given in length value "rungl."; `rungl` = Ladder length. (when pt2=nil); `isvert` = "V" to create a ladder with vertical rungs (i.e. horiz ladder) or "H" to create ladder with horizontal rungs (i.e. vertical ladder). Case sensitive.; `rungw` = Ladder width.; `rungv` = Vertical distance between successive rungs.; `Lc_3sp` = Spacing between 3-ph bus (real value) when Lc_ph = 3.; `rungi` = Rung ref number increment (default = 1).; `gridxy_mode` = nil for line-ref based ladders, or 1 for X-Y grid or X-Zone ladder that will not have line reference numbers attached to it.; `Lc_dr` = -1 to suppress drawing all rungs, 0 draw all, 1 to skip every other rung, 2 skip two, draw one.; `def_1st` = First line ref value (alphanumeric string, not integer).; `options` = nil or 0 to insert normally with line references and side rails.1's bit = suppress line reference numbering.2's bit = suppress side rails.. Example topic: `_c_wd_in_ladder_3_Example.html`. Source: `c_wd_in_ladder.html`.
- `c:wd_ladr_reref` - Freshen ladder reference numbers on active drawing if they include a SHEET attribute and the WD_M block's SHEETvalue differs from that on each ladder's master line reference block insert. Signature: `(c:wd_ladr_reref)`. Source: `c_wd_ladr_reref.html`.
- `c:wd_prnt_time_date` - Add current time and date stamp to title block. Annotate title block with current time and date stamp information if target attributes are present. Signature: `(c:wd_prnt_time_date)`. Source: `c_wd_prnt_time_date.html`.
- `c:wd_read_dwg_params` - Read AutoCAD Electrical's WD_M block for active drawing and fill internal global GBL_wd_m. Insert the WD_M block if it is not found on the active drawing (user is prompted for permission to insert). Signature: `(c:wd_read_dwg_params)`. Returns: Entity name of the WD_M block insert. Source: `c_wd_read_dwg_params.html`.
- `c:wd_ref_col` - Re-reference ladder reference numbers on active drawing. Signature: `(c:wd_ref_col mlr_en first rungi rungv rungc just_erase horv sheet sh_en sub_blk_name)`. Parameters: `mlr_en` = Entity name of the top-most reference number (the master line reference block insert).; `first` = First line reference value (alphanumeric string, not integer).; `rungi` = Line reference rung increment (default = 1).; `rungv` = Vertical distance between sucessive rungs.; `rungc` = Count of rung references to update.; `just_erase` = 1 to blank out value on master line ref (the top-most value) and erase existing 2+ line reference numbers.; `horv` = "V" for ladder with vertical rungs, "H" for ladder with horizontal rungs.; `sheet` = Optional sheet number text value if this is to part of the special user hex block (that has its own SHEET attribute).; `sh_en` = SHEET attrib ent name on the special user master line ref block insert, nil = none.; `sub_blk_name` = Name of nested block embedded into special user hex block, carries the master line ref attributes.. Source: `c_wd_ref_col.html`.
- `c:wd_renum_ladders` - Re-reference ladder reference numbers on active drawing with minimum information required. Signature: `(c:wd_renum_ladders lst)`. Parameters: `lst` = list of one or more sublists, each sublist consisting of a mimimum of two and max of five pieces of information: old_first_num = existing first line ref number on target ladder.new_first_num = replacement for above (three optional parameters, use "nil" if not defined).rungi = optional line ref rung increment (if different from existing).rungv = optional rung spacing value (if different from existing).rungc = optional count of rung refs to update.. Example topic: `_c_wd_renum_ladders_3_Example.html`. Source: `c_wd_renum_ladders.html`.
- `c:wd_reread_dwg_params` - Provided for backwards compatibility. Use wd_read_dwg_params or ace_GBL_wd_m instead. Signature: `(c:wd_reread_dwg_params)`. Source: `c_wd_reread_dwg_params.html`.
- `c:wd_tb_process_one` - Update title block attribute values. Signature: `(c:wd_tb_process_one suppress togglelst)`. Parameters: `suppress` = 1 to suppress any error alert boxes, 0 to allow.; `togglelst` = list of update toggle flags consisting of a 13-element sublist and then followed by a variable length list of LINEn index numbers. First sublist (of 16 elements, 1 for selected, 0 for not selected), described below. After this sublist, give the integer values of the "LINEn" index numbers to process where index value= n-1, that is, "zero-based" (ex: LINE1 index value=0, LINE20 index = 19). This list of LINEn attribute references can be zero to unlimited length. 1st = mapped to DWGDESC (the description under drawing "properties").2nd = mapped to DWGSEC.3rd = mapped to DWGSUB.4th = mapped to FILENAME.5th = mapped to FILENAMEEXT.6th = mapped to FULLFILENAME.7th = mapped to DWGNAM.8th = mapped to SHEET.9th = mapped to SHEETMAX.10th = (not used for single drawing update)11th = mapped to IEC_P (the drawing's IEC coding default for %P).12th = mapped to IEC_I (the drawing's IEC coding default for %I).13th = mapped to IEC_L (the drawing's IEC coding default for %L).14th = mapped to 1st line only of DWGDESC15th = mapped to 2nd line only of DWGDESC16th = mapped to 3rd line only of DWGDESC. Source: `c_wd_tb_process_one.html`.

### Section F - General Functions

General functions.

- Source topic: `Section_F_-_General_Functions.html`
- Routine count: 73

- `ace_publish_create_dsd` - Creates an electrical DSD file, for an active project, that can be used to publish a list of drawing layouts to a PDF file using the -PUBLISH command. Signature: `(ace_publish_create_dsd dsd_file_path drawing_list publish_type sheet_type layout_name include_bookmarks include_hyperlinks zoom_factor output_path)`. Parameters: `dsd_file_path` = The DSD file path (string); `drawing_list` = A list of drawing files used for publishing, (list, set it to nil to include all drawing files in the active project); `publish_type` = Publish format type (string, only support PDF, set it to "PDF"); `sheet_type` = The sheet type (string, only support multiple sheets, set it to "MULTI"); `layout_name` = Name of the layout (string, example: "MODEL"); `include_bookmarks` = Whether to include bookmarks. (boolean, set it to T to include bookmarks); `include_hyperlinks` = Whether to include hyperlinks. (boolean, set it to T to include hyperlinks); `zoom_factor` = Defines the zoom factor (Integer, number between 1 to 100); `output_path` = Location in which the output file is to be created (string). Example topic: `_ace_publish_create_dsd_3_Example.html`. Source: `ace_publish_create_dsd.html`.
- `aegetsqlservertimeout` - Returns the currently set SQL server timeout value when called in SQL server mode only. Returns -1 If called in Access mode. Signature: `(aegetsqlservertimeout)`. Returns: If called in SQL server mode, an integer representing the current timeout value is returned in seconds, otherwise -1. Source: `aegetsqlservertimeout.html`.
- `aesetsqlservertimeout` - Sets the SQL server timeout value when called in SQL server mode. This timeout determines how long to wait before SQL connection or query attempts are considered failed. Setting this higher in poor network conditions will result in longer waiting but might help to avoid connection errors. Timeout defaults to 30 seconds and persists across sessions. Does nothing and returns nil if called in Access mode. Signature: `(aesetsqlservertimeout timeout)`. Parameters: `timeout` = Integer value between 15 and 500 (seconds).. Returns: T if successful; nil if unsuccessful. Example topic: `_aesetsqlservertimeout_3_Example.html`. Source: `aesetsqlservertimeout.html`.
- `c:acadelm` - Moves entities to new layer, even entities that are embedded in inserted blocks on the drawing. Signature: `(c:acadelm)`. Source: `c_acadelm.html`.
- `c:acadex` - Use for troubleshooting gap pointers and other entities with extended entity pointer data. Signature: `(c:acadex)`. Source: `c_acadex.html`.
- `c:ace_chgjst` - Change justification of target attribute, text entity, or attribute definition entity. Signature: `(c:ace_chgjst en jst)`. Parameters: `en` = Entity name of attribute, text, or attribute definition.; `jst` = justification code (examples "L", "C", "R", "TL"...). Returns: nil=failure, 1=success Example topic: `_c_ace_chgjst_3_Example.html`. Source: `c_ace_chgjst.html`.
- `c:ace_dir_files` - Perform a search of AutoCAD Electrical folders for files, wild-card matching. Signature: `(c:ace_dir_files pattern options)`. Parameters: `pattern` = Wildcard pattern to match, for example, "*.xls".; `options` = nil or 0 to use base AutoCAD Electrical search path sequence (user folder, current project's folder, base folder and Support folder in "program files", base Support folder) and AeData folder in "documents and settings").1's bit set - include search of the "...catalogs" folder.2's bit set - include search of the "...plc" folder.4's bit set - include search of AutoCAD paths.. Returns: List of all files found with full qualified path/filenames, or nil if none found. Source: `c_ace_dir_files.html`.
- `c:ace_find_file` - Find AutoCAD Electrical support file. Signature: `(c:ace_find_file nam options)`. Parameters: `nam` = file name to find (for example, "ace_plc.mdb").; `options` = nil or 0 to use normal AutoCAD Electrical search path sequence (see wd_does_block_exist).1's bit set - include search of the "...catalogs" folder.2's bit set - include search of the "...plc" folder.16's bit set - display "file not found / path list" dialog box if file not found.. Source: `c_ace_find_file.html`.
- `c:ace_find_substr` - Find a substring. Note: this is NOT case sensitive. Signature: `(c:ace_find_substr string sub startix)`. Parameters: `string` = Overall string to search.; `sub` = Substring to find within "string".; `startix` = char starting point in "string" to start search from (1=1st character position).. Returns: nil if there is no match, or character position where sub match begins (1=1st character position). Source: `c_ace_find_substr.html`.
- `c:ace_get_1000_xdata_dbx` - Get XDATA (1000) value from block INSERT instance on drawing. Signature: `(c:ace_get_1000_xdata_dbx dnam hdl appnam options)`. Parameters: `dnam` = nil for active dwg or full path/dwg name (use double backslashes or single forward slashes).; `hdl` = AutoCAD entity handle of block insert instance.; `appnam` = Full Xdata name to search for.; `options` = For future use.. Returns: Xdata record's "1000" data, or nil if not found. Source: `c_ace_get_1000_xdata_dbx.html`.
- `c:ace_get_cable_conductor_data` - Find cable MFG/CAT entry in the _W0_CBLWIRES table of the catalog lookup database file. Return two parallel lists; a list of cable conductor colors and a list of cable conductor gauge values. Signature: `(c:ace_get_cable_conductor_data param_lst)`. Parameters: `param_lst` = List of parameters: nth 0 = MFG manufacturer codenth 0 = MFG manufacturer codenth 1 = CAT cable catalog number. Returns: list of returned data (or nil if no catalog match found). nth 0 = list of cable color values. nth 1 = list of cable gauge values. Example topic: `_c_ace_get_cable_conductor_data_3_Example.html`. Source: `c_ace_get_cable_conductor_data.html`.
- `c:ace_get_cat_data` - Do a catalog lookup query and return matching record(s). Signature: `(c:ace_get_cat_data options mfg pn asm wdblknam)`. Parameters: `options` = For future use.; `mfg` = Manufacturer code (can be blank).; `pn` = Catalog part number.; `asm` = ASSYCODE value (can be blank).; `wdblknam` = Symbol block name (or WDBLKNAM value).. Returns: nil if no record found, otherwise returns (list ...) where data_lst: nth 0 = "0" for main part number record, "" for subsequent sub-assembly records nth 1 = quantity nth 2 = subassy count nth 3 = catalog part number nth 4 = manufacturer nth 5 = ASSYCODE value nth 6 = "" (not used) Example topic: `_c_ace_get_cat_data_3_Example.html`. Source: `c_ace_get_cat_data.html`.
- `c:ace_get_pnlval_dbx` - Get ATTRIB or XDATA (1000) value from block INSERT instance on drawing. XDATA is in form "VIA_WD_attname". Look for ATTRIB first, if not found, look for XDATA value. Signature: `(c:ace_get_pnlval_dbx dnam hdl attnam options)`. Parameters: `dnam` = nil for active dwg or full path/dwg name (use double backslashes or single forward slashes); `hdl` = AutoCAD entity handle of block insert instance.; `attnam` = Attribute's name to query for its value.; `options` = For future use.. Returns: Return nil if not found, otherwise return value Example topic: `_c_ace_get_pnlval_dbx_3_Example.html`. Source: `c_ace_get_pnlval_dbx.html`.
- `c:ace_get_ptag_attrval` - Find panel footprint symbols or panel terminal attribute value or split tag values from block insert instance. Find P_TAG1, P_TAG1_PART* (panel footprint symbols), or P_TAGSTRIP (panel terminal) attribute value or split tag values from block insert instance. Look for Xdata if target attributes are not found. Signature: `(c:ace_get_ptag_attrval ben options_lst)`. Parameters: `ben` = Entity name of block insert.; `options_lst` = nil (for future use).. Returns: nil=failure. If single attribute = (list tagstr 1 what attrnam attr_entname). If split-tag attributes = (list tagstr cnt what attrnam_lst attr_entname_lst attr_val_lst) where: "tagstr" = full tag-id string (concatenated if split tag) "cnt" = 1 for single attribute, 2 or 3 for split tag "what" = 1 for component fooptrint, 0=panel terminal or unknown "attrnam" = tag attribute name (ex: "P_TAG1") "attr_entname" = entity name of the tag attribute "attrnam_lst" = list of 3 split attribute names "attr_entname_lst" = list of split attribute entity names (nil values= saved as Xdata) "attr_val_lst" = value list of individual split attributes Source: `c_ace_get_ptag_attrval.html`.
- `c:ace_get_tag_attrval` - Get tag attribute values. Find TAG1, TAG1F, TAG1_PART*, TAG1F_PART*, (parent schematic symbols) or TAG2, TAG2_PART* (child schematic symbols) or TAG, TAG_* (PLCIO and divided PLCIO children) or TAGSTRIP (schematic terminal) attribute value or split tag values from block insert instance. Signature: `(c:ace_get_tag_attrval ben options_lst)`. Parameters: `ben` = Entity name of block insert.; `options_lst` = nil (for future use).. Returns: (if single attribute) = (list tagstr 1 what attrnam attr_entname). (if split-tag attributes) = (list tagstr cnt what attrnam_lst attr_entname_lst attr_val_lst) where:"tagstr" = full tag-id string (concatenated if split tag) "cnt" = 1 for single attribute, 2 or 3 for split tag "what" = 1 for parent, 2 for child, 0=terminal or unknown "attrnam" = tag attribute name (ex: "TAG1" "attr_entname" = entity name of the tag attribute "attrnam_lst" = list of 3 split attribute names "attr_entname_lst" = list of split attribute entity names "attr_val_lst" = value list of individual split attributes Source: `c_ace_get_tag_attrval.html`.
- `c:ace_get_textvals` - Retrieve catalog record's TEXTVALS data, if defined. ONE of the first two arguments must be supplied. Signature: `(c:ace_get_textvals ben blknam mfg cat asmb)`. Parameters: `ben` = Target entity name or entity handle.; `blknam` = Target entity block name.; `mfg` = manufacturer code (can be blank); `cat` = catalog part number; `asmb` = ASSYCODE value (can be blank). Source: `c_ace_get_textvals.html`.
- `c:ace_getattr_data` - Get specific attribute information from block insert. Signature: `(c:ace_getattr_data ben attrnam)`. Parameters: `ben` = AutoCAD entity name of selected AutoCAD Electrical component block.; `attrnam` = AutoCAD attribute to extract (wild cards supported).. Returns: (list attrvalue attrname attr_ent_name) or nil if failure. Example topic: `_c_ace_getattr_data_3_Example.html`. Source: `c_ace_getattr_data.html`.
- `c:ace_mod_tag_attrval` - Find tag attribute on schematic symbol and then write modified tag-ID value out to it. If the symbol carries a "split" tag, then divide the "tagstr" across the split tag attributes. Tag attribute can be single TAG1, TAG2, TAG1F, TAG, TAG_*, TAGSTRIP or can be split tags TAG1_PART*, TAG2_PART*. Signature: `(c:ace_mod_tag_attrval ben tagstr options_lst)`. Parameters: `ben` = Schematic component entity to process.; `tagstr` = New tag-ID value.; `options_lst` = nil (for future use).. Returns: nil if failure, 1=success Source: `c_ace_mod_tag_attrval.html`.
- `c:ace_projwide_script` - Run a user script against each drawing to be processed. Similar to selecting the "Run command file script" option of the Project-Wide Utilities (AEUTILITIES) user command. The script is run against the listed drawings, but all user prompts are bypassed. Signature: `(c:ace_projwide_script dlst scrfnam)`. Parameters: `dlst` = nil to process all dwgs in active project, or list of dwgs to process; `scrfnam` = script file name to run against each drawing. Source: `c_ace_projwide_script.html`.
- `c:ace_surf_zoom_in_no_dlg` - Force surf to drawing, zoom in on entity, and optionally center on coordinates. Signature: `(c:ace_surf_zoom_in_no_dlg fnam hdl optional_xy)`. Parameters: `fnam` = Full path to target dwg, nil=active drawing.; `hdl` = Handle of entity to zoom up on.; `optional_xy` = nil to zoom up on insertion point of object "hdl", otherwise zoom up on this coordinate.. Source: `c_ace_surf_zoom_in_no_dlg.html`.
- `c:ace_tag_fixed` - Change block insert's "tag-ID" value to fixed or back to normal. Block can be a wire number or a parent schematic component. Signature: `(c:ace_tag_fixed en fixed_flag options)`. Parameters: `en` = Entity name of block insert to process.; `fixed_flag` = 1 to force tag to "Fixed", otherwise restore to "Normal"; `options` = 1's bit set = "read only" mode, return current fixed/normal state but do not update (ignore state of "fixed_flag"). Returns: RETURN (list return_state at_en ben value tagname) [tagname for component only] ; where "return_state" = nil if not component or wire number or no change required, ; 0 = changed to normal or reporting normal, 1 = fixed ; "at_en" = entity name of tag-ID or wire number attribute ; "ben" = entity name of component or wire number insert ; "value" = text value of tag-ID or wire number ; "tagname" = tag-ID attribute name (components only) - ex: "TAG1" Source: `c_ace_tag_fixed.html`.
- `c:wd_2ss` - Trigger generation of Export DWGS-->TO Spreadsheet data dump without any user prompts. Signature: `(c:wd_2ss param_lst)`. Parameters: `param_lst` = a "list" of eight elements -dumpwhat = "comp" for schem components, "pnl" for panel components, "term" for schematic terminals, "pnlterm" for panel terminals, "sig" for wire from/to signals, "wnum" for wire numbers, "wnumlay" for wire layers, "plc" for PLC modules, "plcdesc" for PLC I/O points and descriptions.doall = 1 to process all dwgs in current project, nil to do current dwg only, 2=process partial list of dwg indexes (list in partial_lst below).partial_lst = if doall=2 then this gives list of drawing index numbers to process, where 1 = first dwg in project list, 2 = 2nd, and so on, for example, (list "2" "10" "35" "36").todolocs = 1 to process all components, 2 process only components that have a blank location code, 3 process components that "wild card match with "doloc" parameter below.doloc = location code to include (if todolocs = 3). Can have wild cards per AutoCAD standards (for example, "*CAB*") means any location with "CAB" substring.format = output file format: "XLS" for Excel, "MDB" for Microsoft Access format, "CSV" for comma- delimited ASCIIoutfilename = output file name (use double backslashes or single forward slashes).scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).. Example topic: `_c_wd_2ss_3_Example.html`. Source: `c_wd_2ss.html`.
- `c:wd_4_squish_fnam` - Replace center characters in filename string with "..." Strip characters out of the middle of file name and replace with "..." in order to make file name text fit into required number of characters. Signature: `(c:wd_4_squish_fnam fname maxwdt)`. Parameters: `fname` = Filename string to squish.; `maxwdt` = Maximum number of characters for result string.. Returns: Filename string reduced to required length with "..." in the center. Example topic: `_c_wd_4_squish_fnam_3_Example.html`. Source: `c_wd_4_squish_fnam.html`.
- `c:wd_add_attr` - Add an attribute to an existing inserted block on a drawing. Signature: `(c:wd_add_attr entnam attnam val jst hgt wdt xy visibility)`. Parameters: `entnam` = Block entity name (nil = prompts user to pick block).; `attnam` = Attribute name to add (nil = prompts for name).; `val` = Value to attach to new attribute (nil = prompts for value).; `jst` = Justification (ex: "L" = left) (nil = defaults to "L").; `hgt` = Attribute text height (nil = defaults to TEXTSIZE variable).; `wdt` = Width (nil = 1.0).; `xy` = Attrib insertion point xyz (nil = prompt user for insertion point).; `visibility` = 1 for invisible, 0 for visible.. Returns: Entity name of the re-written block insert instance. Source: `c_wd_add_attr.html`.
- `c:wd_block_path_failed_msg` - Display error dialog box for block not found in AutoCAD Electrical search paths. Signature: `(c:wd_block_path_failed_msg nam)`. Parameters: `nam` = Name of block not found in search paths.. Source: `c_wd_block_path_failed_msg.html`.
- `c:wd_bomr_ent_qb` - Display full catalog information for select schematic or pnl block. Signature: `(c:wd_bomr_ent_qb ben)`. Parameters: `ben` = AutoCAD entity name of selected AutoCAD Electrical schematic or pnl footprint block.. Example topic: `_c_wd_bomr_ent_qb_3_Example.html`. Source: `c_wd_bomr_ent_qb.html`.
- `c:wd_bomr_entcat` - Gather lists of catalog number information from entity and format into a list of lists. Signature: `(c:wd_bomr_entcat ben)`. Parameters: `ben` = selected INSERT entity, AcadE schematic or panel footprint.. Returns: List of lists (rtrn): (nth 0 rtrn) = (list mfg cat assycode count dbfblknam unit_measure) (nth 1 rtrn) = (list (list {2+ multiple BOM cat number lists})) (nth 2 rtrn) = (list {2-char suffixes for each 2+ part num list}) Source: `c_wd_bomr_entcat.html`.
- `c:wd_bomr_qbdat` - Get the list of catalog look-up data lists for the selected entity. Signature: `(c:wd_bomr_qbdat ben)`. Parameters: `ben` = AutoCAD entity name of selected AutoCAD Electrical schematic or pnl footprint block.. Returns: List of lists where first list is for the main part number and any subsequent lists are the "Multi- BOM" extra part number. Each list consists of 17 or more elements including part number, block name, and a full dump of the data from the target catalog look-up file. Source: `c_wd_bomr_qbdat.html`.
- `c:wd_bswap` - Swap out INSERT entity with new block. Preserve attribute values and wire connections. *Note: blocks swapped must have different names. Use c:wd_bswap_redefine_curdwg to swap blocks with the same name. Signature: `(c:wd_bswap oldben wd_symnam options retain_scl force_scl maplst)`. Parameters: `oldben` = Old block entity to swap out.; `wd_symnam` = New block entity.; `options` = 1's bit set = retain old ATTRIB locations, 0 = use new.2's bit set = suppress copy of existing attrib values to matching attribs on new block, 0 = copy values from old to new where there is a match.4's bit set = copy old to new if new's attribute value is blank (attrib MFG*,CAT*,TERM*,PINLIST*, ASSYCODE*,MAXN* attributes only).8's bit set = return handle of swapped block, 0 = return entity name of swapped block.16's bit set = only reconnect to LINES that are on a valid wire layer.nil = 0 or no bits set; `retain_scl` = 1 to keep existing symbol's scale.; `force_scl` = scale value override if non-nil.; `maplst` = nil to ignore or (list (list old new) (list old new)...) to map existing attribute values to new symbol's attribute values.. Returns: (list ent dif_fam) where: "ent" = entity name or handle (based upon 8's bit of options) or nil if nothing swapped. "dif_fam" = 1 if swapped block was of a different FAMILY than the original. Example topic: `_c_wd_bswap_3_Example.html`. Source: `c_wd_bswap.html`.
- `c:wd_bswap_list` - Block swapping, drawing-wide. Connected wires automatically stretch/trim to reconnect, when possible. *Note: blocks swapped must have different names. Use c:wd_bswap_redefine_curdwg to swap blocks with the same name. Signature: `(c:wd_bswap_list oldnams newnams retain retain_scl maplst)`. Parameters: `oldnams` = List of old block names.; `newnams` = List of new block names.; `retain` = Retain old.; `retain_scl` = Retain old scale.; `maplst` = nil or optional list of parallel lists mapping old attrib values to new. For example, (list (list "OLDNAM1" "OLDNAM2" "OLDNAM3") as old names, and (list "TAG#*" "DESC1" DESC2")) as corresponding new names.. Example topic: `_c_wd_bswap_list_3_Example.html`. Source: `c_wd_bswap_list.html`.
- `c:wd_bswap_redefine_curdwg` - Update existing block with a newer version of the same block Signature: `(c:wd_bswap_redefine_curdwg ( oldnam disknam options retain_scl new_scl maplst/ )`. Parameters: `oldnam` = Old block name to swap out.; `disknam` = New block name used to replace; `options` = 1's bit set = retain old ATTRIB locations, 0 = use new.2's bit set = suppress copy of existing attrib values to matching attribs on new block, 0 = copy values from old to new where there is a match.4's bit set = copy old to new if new's attribute value is blank (attrib MFG*,CAT*,TERM*,PINLIST*, ASSYCODE*,MAXN* attributes only).8's bit set = return handle of swapped block, 0 = return entity name of swapped block.16's bit set = only reconnect to LINES that are on a valid wire layer.nil = 0 or no bits set; `retain_scl` = 1 to keep existing symbol's scale.; `new_scl` = scale value override if retain_scl is nil.; `maplst` = nil to ignore or (list (list old new) (list old new)...) to map existing attribute values to new symbol's attribute values.. Example topic: `_c_wd_bswap_redefine_curdwg_3_Example.html`. Source: `c_wd_bswap_redefine_curdwg.html`.
- `c:wd_cat_lookup` - Open AutoCAD Electrical's CATALOG LOOKUP dialog box. Signature: `(c:wd_cat_lookup wdblknam cmp_en mfgn_old pn_old asm_old nocreate)`. Parameters: `wdblknam` = block name to drive table to query (ex: "HLT1R" will access "LT1R").; `cmp_en` = optional entity name to find catalog number for (nil=use wdblknam for determining table name.; `mfgn_old` = Optional catalog info to trigger dialog box to come up with the target catalog entry highlighted (if it exists in the database table).; `pn_old` = Optional catalog info to trigger dialog box to come up with the target catalog entry highlighted (if it exists in the database table).; `asm_old` = Optional catalog info to trigger dialog box to come up with the target catalog entry highlighted (if it exists in the database table).; `nocreate` = 1 to prohibit call from offering user option to create a new database table if the target one does not exist.. Returns: Selected record or nil if none. Source: `c_wd_cat_lookup.html`.
- `c:wd_chg_to_attdef` - Convert text entity to an attrib definition, in place. Note: this does NOT add an attribute to an existing block insert. Signature: `(c:wd_chg_to_attdef txten attnam)`. Parameters: `txten` = Entity name of text entity to convert.; `attnam` = Attrib tag name.. Returns: Entity name of the created attribute definition entity. Source: `c_wd_chg_to_attdef.html`.
- `c:wd_chgjst` - Allows user picks to change ATTRIB or TEXT justification. Can be set up in acad.pgp as a "hot key", if desired (ex: "J, *wd_chgjst") where user types "J", space, , [Enter] and then starts picking. Signature: `(c:wd_chgjst)`. Source: `c_wd_chgjst.html`.
- `c:wd_comp_ref_list` - Return list of cross-reference information for selected component or tag name. Signature: `(c:wd_comp_ref_list en compinfo_lst freshen)`. Parameters: `en` = Component entity name.; `compinfo_lst` = (list tagn inst loc family) (or nil if "en" provided).; `freshen` = 1 to freshen database before doing search for refs.. Returns: List in the following form: (list picked used notused coil_pins used_formc avail_formc pinlist). Where : wirenum1 and wirenum2 = wire number assignments on connected wires on each side of contact. pin1 and pin2 = TERMDESCxx terminal pin description text associated with the two wire connections. typ = component contact type: NO, NC, or <> for coil. ref = line or grid reference location of component. sh and dwgnam = the %S and %D values set up in the AcadE CONFIG dialog box. hdl = AutoCAD object handle value of the component. dwgix = drawing index value in scratch database for this component (field DWGIX). Source: `c_wd_comp_ref_list.html`.
- `c:wd_del_1000_xdata` - Delete an existing "dxf group code 1000" xdata record from an entity. Signature: `(c:wd_del_1000_xdata en appname)`. Parameters: `en` = Entity name.; `appname` = xdata name (for example, "VIA_WD_MFG01").. Returns: 1=success, nil = not found or failure. Source: `c_wd_del_1000_xdata.html`.
- `c:wd_del_xdata_appnam` - Deletes xdata from an entity Signature: `(c:wd_del_xdata_appnam ben appnam)`. Parameters: `ben` = Block Entity; `Appnam` = The xdata name. It requires the VIA_WD_ prefix for the xdata name. Example topic: `_c_wd_del_xdata_appnam_3_Example.html`. Source: `c_wd_del_xdata_appnam.html`.
- `c:wd_delim_str_to_lst` - Convert a delimited string into an AutoLISP list. Signature: `(c:wd_delim_str_to_lst str delim_char)`. Parameters: `str` = String to convert.; `delim_char` = Character acting as delimiter.. Returns: List of parsed strings. Source: `c_wd_delim_str_to_lst.html`.
- `c:wd_does_block_exist` - Search for schematic block name using AutoCAD Electrical's search paths. Signature: `(c:wd_does_block_exist nam)`. Parameters: `nam` = Name of block. Paths are searched in the following order: 1st = checks active drawing for block defined in the drawing itself.2nd = actual name if full path provided.3rd = in USER folder (pointed at by global GBL_wd_usr).4th = in active project's folder (where the ".wdp" is located).5th = in Schematic lib path(s) (pointed at by global GBL_wd_lib).6th = in "c:documents and settings...support" folder.7th = in folder pointed at by global GBL_wd_sup.8th = AutoCAD search paths (this is inserted as the 3rd check if the wd.env file's WD_ACADPATHFIRST setting is enabled).. Returns: Full path name if found, nil otherwise. Source: `c_wd_does_block_exist.html`.
- `c:wd_does_block_exist_dbx` - Return the block name if the block is already defined in drawing. Signature: `(c:wd_does_block_exist_dbx nam ff dwgnam)`. Parameters: `nam` = block name to search for (can include ".dwg" extension) Return block name "nam" if block already defined in drawing. Otherwise search for "nam" in this order: 1st: actual name if full path was provided2nd: in path pointed to by GBL_wd_usr3rd: in subdirectory where current project file is located (if proj active)4th: in path GBL_wd_lib5th: in "c:documents and settings..." support folder6th: in path GBL_wd_sup7th: normal AutoCAD directories; `ff` = handle if target dwg is already opened with (wd_dbx_open...), nil if not; `dwgnam` = (if ff=nil) then = drawing name (nil=active drawing). Returns: Return full path name. If not successful, return nil. Source: `c_wd_does_block_exist_dbx.html`.
- `c:wd_does_file_exist` - Search various AutoCAD Electrical install paths and AutoCAD support path(s) for the file "nam". Signature: `(c:wd_does_file_exist nam)`. Parameters: `nam` = File name.. Returns: Full path name if found, nil otherwise. Example topic: `_c_wd_does_file_exist_3_Example.html`. Source: `c_wd_does_file_exist.html`.
- `c:wd_does_file_exist_error` - Display error dialog box for file name not found in AutoCAD Electrical search paths. Signature: `(c:wd_does_file_exist_error msg nam)`. Parameters: `msg` = Message to display.; `nam` = File name not found in search paths.. Source: `c_wd_does_file_exist_error.html`.
- `c:wd_edit_attribute` - Retrieve target attribute from insert and open an edit dialog box that has "pick just like" and increment/decrement toggles. Signature: `(c:wd_edit_attribute ben attnam)`. Parameters: `ben` = AutoCAD entity name of selected AcadE component block.; `attnam` = AutoCAD attribute to edit (wild cards supported).. Returns: Value of 1 if attribute value changed, nil = no change or attribute not found. Example topic: `_c_wd_edit_attribute_3_Example.html`. Source: `c_wd_edit_attribute.html`.
- `c:wd_find_wdw` - Find and return the file name of the wire color/gauge mapping (.wdw) file appropriate for the current project or else the default file if a project-specific version is not found. Signature: `(c:wd_find_wdw)`. Returns: .wdw file name. Source: `c_wd_find_wdw.html`.
- `c:wd_get_1000_xdata` - Get xdata from entity. Signature: `(c:wd_get_1000_xdata en appname)`. Parameters: `en` = Entity name.; `appname` = xdata name (for example, "VIA_WD_WDBLKNAM").. Returns: xdata record's "1000" data, or nil if not found. Source: `c_wd_get_1000_xdata.html`.
- `c:wd_get_1005_xdata` - Look for handle pointer value saved as xdata on target entity. Signature: `(c:wd_get_1005_xdata en appname)`. Parameters: `en` = Entity name.; `appname` = xdata name (ex: "VIA_WD_WNPTR").. Returns: Handle string if success, nil=target xdata not found. Source: `c_wd_get_1005_xdata.html`.
- `c:wd_get_locs_for_proj` - Extract component LOC values, project-wide. Display in pick list dialog box. Signature: `(c:wd_get_locs_for_proj what)`. Returns: Selected LOC code or nil if nothing picked from dialog box. Source: `c_wd_get_locs_for_proj.html`.
- `c:wd_get_locs_for_proj_mult` - Extract component LOC values, project-wide. Multiple selection. Display in pick list dialog box. Signature: `(c:wd_get_locs_for_proj_mult what)`. Returns: Selected LOC code or codes, comma delimited. Example topic: `_c_wd_get_locs_for_proj_mult_3_Example.html`. Source: `c_wd_get_locs_for_proj_mult.html`.
- `c:wd_get_locs_on_dwg` - Extract component LOC values from the current drawing, display in a pick list dialog box. Signature: `(c:wd_get_locs_on_dwg)`. Parameters: `options` = 1 = display pick list dialog, allow single selection3 = display pick list dialog, allow multiple selection - returns a comma-delimited list. Returns: Selected LOC code or nil if nothing is picked from the dialog box. Source: `c_wd_get_locs_on_dwg.html`.
- `c:wd_get_pnlval` - Get ATTRIB or XDATA (1000) value from panel footprint block "ben" XDATA in form "VIA_WD_attname" (old="CIM_WD_attname"). Look for ATTRIB first, if not found, look for XDATA value. Signature: `(c:wd_get_pnlval ben attnam)`. Parameters: `ben` = AutoCAD entity name of footprint block insert instance.; `attnam` = Attribute's name to query for its value (wildcards ok).. Returns: Attrib or xdata value, or nil if not found. Source: `c_wd_get_pnlval.html`.
- `c:wd_getattrval` - Retrieve attribute value. Signature: `(c:wd_getattrval ben attnam)`. Parameters: `ben` = AutoCAD entity name of selected AutoCAD Electrical component block.; `attnam` = AutoCAD attribute to extract (wild cards supported; capital letters only).. Returns: Attribute value or nil = attribute not found. Example topic: `_c_wd_getattrval_3_Example.html`. Source: `c_wd_getattrval.html`.
- `c:wd_getenv` - Retrieve settings out of "wd.env" environment file. Signature: `(c:wd_getenv varnam)`. Parameters: `varnam` = Variable name (for example, "WD_LIB" or "WD_CAT").. Returns: Character string value or "nil" if not listed. Example topic: `_c_wd_getenv_3_Example.html`. Source: `c_wd_getenv.html`.
- `c:wd_getflist` - Return directory listing list. Signature: `(c:wd_getflist path_fname option)`. Parameters: `path_fname` = Path and or path+wildcard.; `option` = nil (not used).. Returns: List of fully qualified file names, or nil if none found. Source: `c_wd_getflist.html`.
- `c:wd_import_scratch_mdb_doit` - Trigger auto update of current drawing's AutoCAD Electrical component and wire number data based upon changed data in the project scratch ".mdb" database file. Signature: `(c:wd_import_scratch_mdb_doit fnam dwgix table_lst options)`. Parameters: `fnam` = Full path name of AcadE project database file (nil = active proj).; `dwgix` = The "DWGIX" field in the target table(s) to identify which records go with the current dwg (the DWGIX field value in FILETIME table).; `table_lst` = List of tables to process, ex: (list "COMP" "TERMS" "PNLCOMP").; `options` = For future use (set to 0).. Source: `c_wd_import_scratch_mdb_doit.html`.
- `c:wd_increment_str` - Increment ASCII string by value (integer, positive or negative). Signature: `(c:wd_increment_str str val)`. Parameters: `str` = Char string to increment.; `val` = Integer count to increment or decrement.. Source: `c_wd_increment_str.html`.
- `c:wd_ins_table` - Display comma delimited file in a report dialog, optionally insert as a table on to the active drawing. Signature: `(c:wd_ins_table fname)`. Parameters: `fname` = Name of comma delimited file.. Example topic: `_c_wd_ins_table_3_Example.html`. Source: `c_wd_ins_table.html`.
- `c:wd_is_it_schem_or_pnl` - Return code indicating the target block insert. Signature: `(c:wd_is_it_schem_or_pnl ben)`. Parameters: `ben` = Block entity.. Returns: Return code as follows : 1 = PLCIO schematic. 2 = wire number block. 3 = master line reference, i.e. ladder. 4 = schematic, parent. 5 = schematic, child. 6 = source or dest arrow. 7 = schematic terminal. 8 = ref only arrow. 9 = panel component or panel terminal. 10= panel nameplate. 11= stand-alone cross-ref symbol. 12= extra wirenum copy. 13= conduit component (C_TAG1 or C_TAG2). 14= conduit marker component (C_TAG). 15= in-line wire number marker or in-line wire label. 16= in-line multi-connection sequence terminal symbol. 17= report table. 18= iec-style angled wire tee connector symbol. nil = unknown. Source: `c_wd_is_it_schem_or_pnl.html`.
- `c:wd_layer_move` - Moves entities to new layer, even entities that are embedded in inserted blocks on the drawing. Prompts for "old" layer and "move to" layer names. It is therefore scriptable. Signature: `(c:wd_layer_move)`. Source: `c_wd_layer_move.html`.
- `c:wd_make_prev_selset` - Create a "Previous" selection set from entity. Signature: `(c:wd_make_prev_selset ben)`. Parameters: `ben` = Entity name or entity handle string.. Returns: Entity name if success and selection set is created. nil on failure. Example topic: `_c_wd_make_prev_selset_3_Example.html`. Source: `c_wd_make_prev_selset.html`.
- `c:wd_mod_1000_xdata` - Add or modify existing "1000" xdata value. Signature: `(c:wd_mod_1000_xdata en appname data)`. Parameters: `en` = Entity name.; `appname` = xdata name (for example, "VIA_WD_WDBLKNAM").; `data` = xdata record's "1000" data.. Source: `c_wd_mod_1000_xdata.html`.
- `c:wd_mod_1005_xdata` - Save handle pointer string on entity under xdata. Create xdata if it does not exist. If it does exist, overwrite with new value. Signature: `(c:wd_mod_1005_xdata en hdl appname)`. Parameters: `en` = Entity name.; `hdl` = Handle pointer string.; `appname` = xdata name (ex: "VIA_WD_WNPTR").. Returns: non-nil=success, nil=failure Source: `c_wd_mod_1005_xdata.html`.
- `c:wd_modattrval` - Modify attribute "attname" with new value "newval" on insert entity "ben". Don't trigger entity regen if "noupd" is 1. Signature: `(c:wd_modattrval ben attnam newval noupd)`. Parameters: `ben` = AutoCAD entity name of selected AutoCAD Electrical component block.; `attnam` = AutoCAD attribute to find and modify (wildcards ok).; `newval` = New attribute value to insert.; `noupd` = 1 to suppress regen, nil to regen block insert to force screen refresh of a changed attribute value.. Returns: 1 if success, nil = failure or target attribute not found on block. Source: `c_wd_modattrval.html`.
- `c:wd_msg` - Return message string from match in message Access database file wd_msg.mdb in currently selected language. Signature: `(c:wd_msg msgcode vals default_msg)`. Parameters: `msgcode` = Key field value for query.; `vals` = nil if none or list of values to insert into "%1", "%2" flags in the encoded message.; `default_msg` = Message to return if no match found in lookup.. Source: `c_wd_msg.html`.
- `c:wd_read_wdw` - Read wire color/gauge mapping file. Signature: `(c:wd_read_wdw fname)`. Parameters: `fname` = full path name to ".wdw" wire color/gauge mapping file.. Returns: nil if file not found or (list (list laynam color-ga label)(list...)) Example topic: `_c_wd_read_wdw_3_Example.html`. Source: `c_wd_read_wdw.html`.
- `c:wd_selsort` - Sorting list of lists routine, multi-level if desired. Signature: `(c:wd_selsort lst function)`. Parameters: `lst` = List or list of lists to sort.; `function` = Sort function.. Returns: Sorted list. Source: `c_wd_selsort.html`.
- `c:wd_split_fnam` - Split file name into list of path, name, extension. Signature: `(c:wd_split_fnam fname)`. Parameters: `fname` = Filename string to parse.. Returns: List of strings. Source: `c_wd_split_fnam.html`.
- `c:wd_ss2_import_curdwg` - Trigger an auto update of current drawing based upon changed data carried in an Excel spreadsheet, Access file, or comma-delimited ascii spreadsheet dump file. Signature: `(c:wd_ss2_import_curdwg fnam upcase options)`. Parameters: `fnam` = xls,mdb, or csv dump from spreadsheet. Must have several key column labels that AutoCAD Electrical keys off of to find matching block for updates. These include "DRAWING" (full drawing path name) and "HDL" (for block insert handle numbers). Run the AutoCAD Electrical "To Spreadsheet" utility to create an example template file. Table or spreadsheet tab MUST be called "Sheet1".; `upcase` = "1" to force all updated data to upper case, "0" = as is.; `options` = 1's bit set: force all changed tags and wire numbers to "Fixed".2's bit set: suppress re-read of data (For example, first drawing of project-wide run). Source: `c_wd_ss2_import_curdwg.html`.
- `c:wd_upd_pnlval` - Update ATTRIB or XDATA (1000) value from block "ben" XDATA in form "VIA_WD_attname" (old="CIM_WD_attname"). Look for ATTRIB first, if not found, look for XDATA value (has a "VIA_WD_" prefix, old="CIM_WD_" prefix. Signature: `(c:wd_upd_pnlval ben attnam newval)`. Parameters: `ben` = AutoCAD entity name of footprint block.; `attnam` = Attribute's name to query for its value (wildcards ok).; `newval` = New attrib value to insert.. Returns: No return value. Source: `c_wd_upd_pnlval.html`.
- `c:wd_wd2p_set_GBL_wd_pnllst` - Project-wide extract of schematic components. Build list GBL_wd_pnllst that is referenced by the SCHEM-->PNL insertion pick list dialog box. Signature: `(c:wd_wd2p_set_GBL_wd_pnllst)`. Source: `c_wd_wd2p_set_GBL_wd_pnllst.html`.
- `c:wd_wd2p_set_GBL_wd_pnltrmlst` - Build list GBL_wd_pnltrmlst that is referenced by the SCHEM TERM-->PNL insertion pick list dialog box. Signature: `(c:wd_wd2p_set_GBL_wd_pnltrmlst)`. Source: `c_wd_wd2p_set_GBL_wd_pnltrmlst.html`.
- `c:wd_wdsurf` - Trigger AutoCAD Electrical Surfer search and surf dialog box. Signature: `(c:wd_wdsurf type freshen str inst loc)`. Parameters: `type` = nil or 0 for components, 1 for signals, 2 for terminals, 3 for plcio, 4 for wire numbers, 5 for catalog number search; `freshen` = 1 to check for database freshen required, nil = skip.; `str` = Text string to surf on (wild cards allowed). Can be tag-id, wire number, catalog number, signal code.; `inst` = Installation code (when IEC tagging mode is active).; `loc` = Location code (when IEC tagging mode is active).. Source: `c_wd_wdsurf.html`.
- `c:wd_xdata` - API call - run a user script against each drawing to be processed. Signature: `(c:wd_xdata)`. Source: `c_wd_xdata.html`.
- `wd_fio_does_pnlblock_exist` - Search for panel footprint block using AcadE's search paths. Signature: `(wd_fio_does_pnlblock_exist nam)`. Parameters: `nam` = Name of block. Note: block name can be passed with one or more subfolder prefix (ex: "ab/pb- pushbuttons/abpb3r"). Paths are searched in the following order: 1st = checks active drawing for block defined in the drawing itself.2nd = actual name if full path provided.3rd = in USER folder (pointed at by global GBL_wd_usr).4th = in active project's folder (where the ".wdp" is located).5th = in Schematic lib path(s) (pointed at by global GBL_wd_lib).6th = in "c:documents and settings...support" folder.7th = in folder pointed at by global GBL_wd_sup.8th = AutoCAD search paths (this is inserted as the 3rd check if the wd.env file's WD_ACADPATHFIRST setting is enabled).. Returns: Full path name if found, nil otherwise. Source: `wd_fio_does_pnlblock_exist.html`.

### Section G - Project Management

Project / project database file.

- Source topic: `Section_G_-_Project_Management.html`
- Routine count: 36

- `ace_closeproject` - Removes the specified project from project manager if it is not the last project. If the project is active, next available project, which may not in the same order as shown in the UI, will be activated before removing it. If the project to be removed is the last project, remove will fail and return nil. Signature: `(ace_closeproject wdpfilepath)`. Parameters: `wdpfilepath` = Full path of project ".wdp" file.. Returns: If successful T is returned, otherwise nil Example topic: `_ace_closeproject_3_Example.html`. Source: `ace_closeproject.html`.
- `ace_getactiveproject` - Get the active project's .wdp file name. Signature: `(ace_getactiveproject)`. Returns: The full file name of the active project's ".wdp" file. Source: `ace_getactiveproject.html`.
- `ace_relativetoabsolute` - Resolve relative paths found for drawing file names in the ".wdp" project file into full paths. Signature: `(ace_relativetoabsolute dwg_relative_path full_proj_wdp_path)`. Parameters: `dwg_relative_path` = drawing relative path (ex: "....0001.dwg"); `full_proj_wdp_path` = full path to project's ".wdp" file location. Returns: Calculated full path to the drawing Example topic: `_ace_relativetoabsolute_3_Example.html`. Source: `ace_relativetoabsolute.html`.
- `c:ace_add_dwg_to_project` - If the drawing is already listed in the project (regardless of path), it is first removed from the list and then added in at the end of the list. Signature: `(c:ace_add_dwg_to_project dwg2add param_lst)`. Parameters: `dwg2add` = File name of dwg to add, or nil = add CURRENT active drawing.; `param_lst` = (nth 0) = "Sec" section code.(nth 1) = "Subsec" code.(nth 2) = "Drawing description" or list of three descriptions.(nth 3) = 1 to suppress project file freshen after drawing is added to project.(nth 4) = subfolder to add the drawing, eg: "sub1/sub2/sub3", "sub1" or nil for root folder. Returns: 1 = success, nil = failure. Source: `c_ace_add_dwg_to_project.html`.
- `c:ace_language_lst` - Retrieve list of language name fields defined in language swap file. Signature: `(c:ace_language_lst param_lst)`. Parameters: `param_lst` = (list fnam) where "fnam" = language file to use, or nil = use default file "wd_lang1.mdb".. Returns: nil if failure, or list of language column field names (to use in c:ace_language_swap function). Source: `c_ace_language_lst.html`.
- `c:ace_language_swap` - Process active drawing only. Signature: `(c:ace_language_swap param_lst)`. Parameters: `param_lst` = List of parameters: nth 0 = todo - 2=process all dwg, 3=prompt user pick, or can pass as list of entity names to processnth 1 = from â€“ the "From" language (exact match with field name in language swap file)nth 2 = to - to "To" languagenth 3 = exact 1= exact match required, 2= substring matchnth 4 = what 1=DESC1 only, 2=DESC2, 3=DESC3, 5=All labels individually, 6=All labels but DESC1-3 concatenated, 8's bit set=include all TEXT entitiesnth 5 = langfile nil=use default "wd_lang1.mdb"nth 6 = logfile nil=create default "trans.log"nth 7 = gotofnam nil=create default "trans.go2". Source: `c_ace_language_swap.html`.
- `c:ace_proj_data` - Read the Access database file and the project .wdp file of the specified project and return a list of lists of project's drawing data in the format outlined for the GBL_wd_prj_ixlst global variable. Signature: `(c:ace_proj_data wdpfnam)`. Parameters: `wdpfnam` = nil for current, active project. Otherwise pass the full path/filename of the project to query.. Source: `c_ace_proj_data.html`.
- `c:ace_proj_dwg_remove` - API call for Project Manager Enhanced Secondary Window (ESW) to display project dwg selection dialog box and then remove selected dwgs from project. Signature: `(c:ace_proj_dwg_remove options wdp_fname dwg_fname)`. Parameters: `options` = 1's bit set means update wdp file if dwg is removed, otherwise just return the new drawing list.; `wdp_fname` = Project wdp file nil, nil=defaul to active project (stored in GBL_wd_prj).; `dwg_fname` = Drawing file name to find and remove from the project.. Returns: nil = failure (dwg not found, wdp file not found). Success = new dwg list returned. Source: `c_ace_proj_dwg_remove.html`.
- `c:ace_proj_new` - Pops open dialog box for file name selection and creates a blank project file. Signature: `(c:ace_proj_new options)`. Parameters: `options` = 1 = make new project "active", nil = create but don't make active.. Returns: Project ".wdp" file name or nil if failure. Source: `c_ace_proj_new.html`.
- `c:ace_proj_recent` - Display dialog box listing recent projects. (carried in ...\user\lastproj.fil) Signature: `(c:ace_proj_recent)`. Returns: Picked project name (or nil = cancel). Example topic: `_c_ace_proj_recent_3_Example.html`. Source: `c_ace_proj_recent.html`.
- `c:ace_update_WFRM2ALL` - Rebuild WFRM2ALL / WNETLST tables to match current state of the project drawings. Signature: `(c:ace_update_WFRM2ALL options)`. Parameters: `options` = nil or 0 to only rebuild these tables if out of date with any of the drawings.1 to force full rebuild of the project's scratch database file (including the WFRM2ALL and WNETLST tables).. Source: `c_ace_update_WFRM2ALL.html`.
- `c:wd_cpyprj_main` - Copy project utility. At conclusion, the new copied project is the active one and the first drawing listed in the new project is opened. Signature: `(c:wd_cpyprj_main param_lst)`. Parameters: `param_lst` = List of these elements.nth 0 = Proj ".wdp" to copy from (nil=current project).nth 1 = New proj ".wdp" to create (any existing will be overwritten!). Cannot be nil or empty string.nth 2 = dlst (list of dwgs in "copy from" proj to copy, nil=copy all).nth 3 = target "base" folder for copied dwgs. Cannot be nil or empty string.nth 4 = optional "FIND" string for renaming dwg names (case insensitive).nth 5 = optional "REPLACE with" string for renaming dwgs in copied project.nth 6 = optional list of flags for related files to copy, "0" or "1" (see nth 7 below).nth 7 = list of file names for above flags. Order is as follows.nth 8 = optional flag to skip opening of the first dwg from a copied project after the copy operation completes, (nil=continue, 1=skip).See Remarks for list of file names for above flags.. Source: `c_wd_cpyprj_main.html`.
- `c:wd_delprj_main` - Delete project .wdp file and optionally all associated dwg files referenced in the .wdp file. Signature: `(c:wd_delprj_main param_lst)`. Parameters: `param_lst` = nil for user dialog box prompting OR list of these elements for driving by command line.(nth 0 ) = Proj ".wdp" to erase (Note: you cannot delete a project while it is active).(nth 1 ) = options, 1's bit set = delete only ".wdp" file. 1's bit clear = delete wdp file AND all the drawings referenced in the wdp file.. Source: `c_wd_delprj_main.html`.
- `c:wd_get_recent_prj_lst` - Get list of recent project file names and descriptions from userlastproj.fil. Signature: `(c:wd_get_recent_prj_lst)`. Returns: List (rtrn) : (nth 0 rtrn) = list of project ".wdp" file names starting with most recent. (nth 1 rtrn) = formatted list of names for display in selection dialog box (internal use). (nth 2 rtrn) = internal use. (nth 3 rtrn) = parallel list of the first four project description lines for each project in the first element (list (list line1 line2 line3 line4)(list â€¦)). (nth 4 rtrn) = list of Project Manager ESW status codes, "2"=active, "1"=open, "0"=not displayed. (nth 5 rtrn) = ".wdp" name of the next "open" but not active project in the ESW list (nil=no more in list). Source: `c_wd_get_recent_prj_lst.html`.
- `c:wd_is_cur_dwg_in_proj` - Determine if the active drawing is listed in the active project. If so, get which drawing it is in the sequence of listed project drawings (1=1st). Signature: `(c:wd_is_cur_dwg_in_proj)`. Returns: Return value is an integer. Source: `c_wd_is_cur_dwg_in_proj.html`.
- `c:wd_make_newp` - Create new AutoCAD Electrical project (old version). Opens dialog box for file name selection, creates a blank project file, and makes it active. Signature: `(c:wd_make_newp)`. Returns: Project ".wdp" file name or nil if failure. Source: `c_wd_make_newp.html`.
- `c:wd_makeproj_current` - Make specified project the active project. Signature: `(c:wd_makeproj_current wdpfnam)`. Parameters: `wdpfnam` = Full path of project ".wdp" file name. This project does not have to be displayed in the list (i.e. does not have to be "open").. Returns: List of project's parameters (or nil = failure). Source: `c_wd_makeproj_current.html`.
- `c:wd_mdb_freshen` - Force freshen of Access database file for project. Signature: `(c:wd_mdb_freshen wdpfnam)`. Parameters: `wdpfnam` = Project file name or nil to default to current project.. Source: `c_wd_mdb_freshen.html`.
- `c:wd_mdb_freshen_noask` - Same as wd_mdb_freshen but suppresses any "OK to Freshen?" dialog box. Signature: `(c:wd_mdb_freshen_noask wdpfnam)`. Parameters: `wdpfnam` = Project file name or nil to default to current project.. Source: `c_wd_mdb_freshen_noask.html`.
- `c:wd_mdb_get_proj_scratch_dbnam` - Calculate name for project's scratch database file. Signature: `(c:wd_mdb_get_proj_scratch_dbnam wdpfilenam)`. Parameters: `wdpfilenam` = File name of the project's scratch database file. "nil"=get current proj mdb file name. Example topic: `_c_wd_mdb_get_proj_scratch_dbnam_3_Example.html`. Source: `c_wd_mdb_get_proj_scratch_dbnam.html`.
- `c:wd_mdb_rebuild` - Force total rebuild of Access database file for specified project. Note: The preferred method is to use ace_update_WFRM2ALL instead. Signature: `(c:wd_mdb_rebuild dbfnam)`. Parameters: `dbfnam` = nil to default to current project.. Source: `c_wd_mdb_rebuild.html`.
- `c:wd_mdb_update_wfrm2all_netlst_tabs` - Rebuild from current state of the wire connection extraction table data in table WDATA. Signature: `(c:wd_mdb_update_wfrm2all_netlst_tabs options)`. Parameters: `options` = nil (for future use). Source: `c_wd_mdb_update_wfrm2all_netlst_tabs.html`.
- `c:wd_pdwgs` - Pick from list and return list of picked drawing file names. Signature: `(c:wd_pdwgs d_lst main_title title2 exclude_refonly)`. Parameters: `d_lst` = (nth 6 (c:wd_proj_wdp_data)))) or(nth 5 (c:wd_proj_wdp_data_fnam wdpfnam)); `main_title` = Description text for dialog box.; `title2` = Description text for dialog box.; `exclude_refonly` = 1 to hide drawings that are marked "REF ONLY".. Returns: List (rtrn): (nth 0 rtrn) = list of selected drawing file names. (nth 1 rtrn) = list of index numbers Source: `c_wd_pdwgs.html`.
- `c:wd_proj_chkrepeat` - Check whether the specified file name is in the specified file name list. Signature: `(c:wd_proj_chkrepeat fnam d_lst)`. Parameters: `fnam` = Drawing name.; `d_lst` = List of drawings in format as returned by c:wd_proj_wdp_data, - (nth 6 rtrn). Source: `c_wd_proj_chkrepeat.html`.
- `c:wd_proj_dnext` - Automatically QSAVE active drawing if something in database has changed (unless the WD_PROMPT_SAVE setting is set in the wd.env environment file. Signature: `(c:wd_proj_dnext)`. Source: `c_wd_proj_dnext.html`.
- `c:wd_proj_dprev` - Automatically QSAVE active drawing if something in database has changed (unless the WD_PROMPT_SAVE setting is set in the wd.env environment file. Signature: `(c:wd_proj_dprev)`. Source: `c_wd_proj_dprev.html`.
- `c:wd_proj_getdwgnam` - Get list of file names of drawings in project and count drawings in the project file. Signature: `(c:wd_proj_getdwgnam wdpfnam index options)`. Parameters: `wdpfnam` = Project wdp name, or nil = current.; `index` = 0 for 1st dwg in project dwg list, 1=2nd dwg, 2=3rd and so on.; `options` = 0 to return file name and dwg cnt, 1 = execute script to open the drawing (no return data).. Returns: File name of drawing in specified project. Example topic: `_c_wd_proj_getdwgnam_3_Example.html`. Source: `c_wd_proj_getdwgnam.html`.
- `c:wd_proj_wdp_data` - Read active project's WDP file. Signature: `(c:wd_proj_wdp_data)`. Returns: List of lists (rtrn): (nth 0 rtrn) = wdp file name. (nth 1 rtrn) = count of dwgs in file. (nth 2 rtrn) = list of *[ ] TITLE lines (start at (nth 1)). (nth 3 rtrn) = list of ?[ ] values. (nth 4 rtrn) = list of +[ ] values. (nth 5 rtrn) = list of drawing names (absolute paths). (nth 6 rtrn) = list of dwg names (absolute paths) with sec/subsec data included. (nth 7 rtrn) = list of drawing names (absolute paths) with "Ref only" removed. (nth 8 rtrn) = internal use. (nth 9 rtrn) = same as (nth 5 rtrn) but forced upper case. (nth 10 rtrn) = sorted list of all the drawings in the project together with its subfolder data. Example topic: `_c_wd_proj_wdp_data_3_Example.html`. Source: `c_wd_proj_wdp_data.html`.
- `c:wd_proj_wdp_data_fnam` - Read project .wdp file. Signature: `(c:wd_proj_wdp_data_fnam wdpfnam)`. Parameters: `wdpfnam` = .wdp file name to read.. Returns: List of lists (rtrn): (nth 0 rtrn) = count of dwgs in file. (nth 1 rtrn) = list of *[ ] TITLE lines (start at (nth 1)). (nth 2 rtrn) = list of ?[ ] values. (nth 3 rtrn) = list of +[ ] values. (nth 4 rtrn) = list of drawing names (absolute paths). (nth 5 rtrn) = list of dwg names (absolute paths) with sec/subsec data included. (nth 6 rtrn) = list of drawing names (absolute paths) with "Ref only" removed. (nth 7 rtrn) = internal use. (nth 8 rtrn) = same as (nth 4 rtrn) but forced upper case. Source: `c_wd_proj_wdp_data_fnam.html`.
- `c:wd_proj_wdp_write` - Create WDP file from lists of list values. Signature: `(c:wd_proj_wdp_write wdpfnam t_lst a_lst p_lst d2_lst)`. Parameters: `wdpfnam` = Name of .wdp file to create.; `t_lst` = List of title lines for *[ ] entries.; `a_lst` = List of parameter values for ?[ ] entries.; `p_lst` = List of paths and miscellaneous project values for +[ ] entries.; `d2_lst` = List of dwg name with sec/subsec data included.. Example topic: `_c_wd_proj_wdp_write_3_Example.html`. Source: `c_wd_proj_wdp_write.html`.
- `c:wd_reset_proj_vars` - Reset program's internal project global variables and lists. Signature: `(c:wd_reset_proj_vars)`. Source: `c_wd_reset_proj_vars.html`.
- `c:wd_savproj_nam` - Save specified project file name in the standard LASTPROJ.FIL data file. It then appears in the "Recent..." projects pick list dialog box. Signature: `(c:wd_savproj_nam wdpfnam)`. Parameters: `wdpfnam` = project .WDP file name.. Source: `c_wd_savproj_nam.html`.
- `wd_mdb_ix2wdp_ix` - Map the project scratch database file's DWGIX field value back to the position of the drawing reference in the project drawing list. Signature: `(wd_mdb_ix2wdp_ix dwgix)`. Parameters: `dwgix` = Project scratch database file's DWGIX field value.. Returns: 0 if not found, >0 = position in project drawing list. Source: `wd_mdb_ix2wdp_ix.html`.
- `wd_mdb_wdp2ix_ix` - Get the project scratch database DWGIX field value identifier for the drawing in the specified position in the active project's drawing list. Signature: `(wd_mdb_wdp2ix_ix position)`. Parameters: `position` = Integer number, beginning with 1 for 1st drawing in the project list.. Returns: The project scratch database DWGIX field value identifier. Source: `wd_mdb_wdp2ix_ix.html`.
- `wd_mdb2_upd_prj_ixlst` - Set or update the value of variable GBL_wd_prj_ixlst, a list of lists of the active projectâ€™s drawing data. Signature: `(wd_mdb2_upd_prj_ixlst)`. Source: `wd_mdb2_upd_prj_ixlst.html`.
- `wd_pds_get_open_dwgs` - Get file name list of open drawings. Signature: `(wd_pds_get_open_dwgs)`. Returns: File name list of open drawings with the first one in the list being the active drawing. Source: `wd_pds_get_open_dwgs.html`.

### Section H - Reports

Report generation.

- Source topic: `Section_H_-_Reports.html`
- Routine count: 19

- `c:ace_auto_rpt` - Initiate automatic report generation. Signature: `(c:ace_auto_rpt rgfnam firstdnam templatenam)`. Parameters: `rgfnam` = Report Grouping file name as created in Automatic Reports dialog box.; `firstdnam` = Name of first drawing name if table output is an option and new drawing(s) will be created, can be nil if no new drawings will be created.; `templatenam` = Template name if table output is an option and new drawing(s) will be created, nil for no template.. Source: `c_ace_auto_rpt.html`.
- `c:ace_dwglst_rpt_dbx` - Execute and display the Drawing List report. Signature: `(c:ace_dwglst_rpt_dbx dist set_file T)`. Parameters: `dist` = List of drawings from active project.; `set_file` = Format file (.set) name or nil for none.; `T` = This should always be set to T.. Source: `c_ace_dwglst_rpt_dbx.html`.
- `c:ace_termplan_r` - Cause schematic "terminal plan" connection report generation without any user prompts. Signature: `(c:ace_termplan_r param_lst)`. Parameters: `param_lst` = A "list" of nine elements:doall = nil to do active drawing only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic connector symbols; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil").dlst = (ignore if doall = nil). nil = do all drawings in project, otherwise do this list of dwgs in the current project (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).. Source: `c_ace_termplan_r.html`.
- `c:wd_bompnlr` - Cause panel BOM report generation without any user prompts. Signature: `(c:wd_bompnlr param_lst)`. Parameters: `param_lst` = A "list" of ten elements:doall = 1 to process all dwgs in active project; nil to do active dwg only.todolocs = 1 to process all components; 2 process only components that have a blank location code; 3 process components that "wild card match with "doloc" parameter below.doloc = location code to include (if todolocs=3). Can have wild cards per AutoCAD stds (for example, "*CAB*" means any location with "CAB" substring).incl = what to include (0 = pnl comps only, 2 = incl connectors, 4 = incl pnl terms, 8 = incl nameplates, 16 = incl unreferenced schematic comps, 31 = include all Electrical comps, 64 = include Inventor comps, 127 = include all, 1024 = list terminal numbers).purch = tally in purchase order format; 1=purchase order format, 0=normal BOM format, 2 = "By TAG" BOM formatformat = output file format "ASC" for ASCII, "XLS" for Excel, "MDB" for Access, "XML," "CSV" for comma-delimited ASCII, "CSV+P" for comma-delimited ASCII with project info as first line of data.fields = 0 to output all fields; 1= output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref â€˜FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_bompnlr.html`.
- `c:wd_bomr` - This function is obsolete - use wd_bomschr instead. Signature: `(c:wd_bomr)`. Source: `c_wd_bomr.html`.
- `c:wd_bomschr` - Cause schematic BOM report generation without any user prompts. Signature: `(c:wd_bomschr param_lst)`. Parameters: `param_lst` = A "list" of ten elements:doall = 1 to process all dwgs in active project; nil to do active dwg only.todolocs = 1 to process all components; 2 process only components that have a blank location code; 3 process components that "wild card match with "doloc" parameter below.doloc = location code to include (if todolocs=3). Can have wild cards per AutoCAD stds (for example, "*CAB*" means any location with "CAB" substring).incl = what to include (0 = sch comps only, 1 = incl cables, 2 = incl connectors, 3 = include all Electrical comps, 64 = include Inventor comps, 127 = include all, 1024 = list terminal numbers).purch = 1 = like subitems combined, nil = normal tallied BOM, 2 = "By TAG" BOM, 3=normal tallied BOM grouped by Inst/Loc.format = output file format "ASC" for ASCII, "XLS" for Microsoft Excel format, "MDB" for Microsoft Access format, "CSV" for comma-delimited ASCII, "CSV+P" for comma-delimited ASCII with project info as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required; use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP variables: for AutoCAD Release 14 it is simply FNAM, for AutoCAD 2000 the report file name can be retrieved via the following AutoLISP expression: (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_bomschr.html`.
- `c:wd_chkpwr_r` - Cause generation of Power source/load data report without any user prompts. Signature: `(c:wd_chkpwr_r param_lst)`. Parameters: `param_lst` = A "list" of elements:dosingle = 1 to do active dwg only.todolocs = 1 to process all components; 2 process only components that have a blank location code; 3 process components that "wild card match with "doloc" parameter below.doloc = location code to include (if todolocs = 3). Can have wild cards per AutoCAD stds (for example, "*CAB*" means any location with "CAB" substring).format = output file format "XLS" for Excel, "MDB" for Microsoft Access format, "CSV" for comma delim ascii.future = 0outfilename = output file name (use double backslashes or single forward slashes)future = nil. Example topic: `_c_wd_chkpwr_r_3_Example.html`. Source: `c_wd_chkpwr_r.html`.
- `c:wd_compconn_r` - Cause schematic component connection report generation without any user prompts. Signature: `(c:wd_compconn_r param_lst)`. Parameters: `param_lst` = A "list" of nine elements:doall = nil to do active drawing only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic connector symbols; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil").dlst = (ignore if doall = nil). nil = do all drawings in project, otherwise do this list of dwgs in the current project (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).. Example topic: `_c_wd_compconn_r_3_Example.html`. Source: `c_wd_compconn_r.html`.
- `c:wd_compr_pnlr` - Cause panel component report generation without any user prompts. Signature: `(c:wd_compr_pnlr param_lst)`. Parameters: `param_lst` = A "list" of nine elements:doall = 1 to process all dwgs in active project; nil to process active dwg only.todolocs = 1 to process all components; 2 process only components that have a blank location code; 3 process components subject to wild card match with "doloc" parameter.doloc = location code to include (if todolocs = 3). Can have wild cards per AutoCAD standards (for example, "*CAB*" means any location with "CAB" substring).incl = what to include (0 = components, 1's bit = name plates, 2's bit = connectors, 4's bit = panel terminals).suprs = suppress blank entries (0 = extract all, 1 = suppress blank TAG items, 2 = suppress blank MFG/CAT items, 3 = suppress both 1 and 2).format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).. Source: `c_wd_compr_pnlr.html`.
- `c:wd_compr_sch` - Cause schematic component report generation without any user prompts. Signature: `(c:wd_compr_sch param_lst)`. Parameters: `param_lst` = A "list" of ten elements:doall = 1 to process all dwgs in current project; nil to process current dwg only.todolocs = 1 to process all components; 2 process only components that have a blank location code; 3 process components subject to wild card match with "doloc" parameter.doloc = location code to include (if todolocs=3). Can have wild cards per AutoCAD standard (for example, "*CAB*" means any location with "CAB" substring).incl = what to include (nil = all, 1's bit = parent components, 2's bit = parent and child components, 4's bit = cable marker symbols, 8's bit = connector symbols).suprs = suppress blank entries (0 = extract all, 1 = suppress blank TAG items, 2 = suppress blank MFG/CAT items, 3 = suppress both 1 and 2).format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_compr_sch.html`.
- `c:wd_connpin_rpt1` - Cause "Connector pin - Summary" report generation without any user prompts. Signature: `(c:wd_connpin_rpt1 param_lst)`. Parameters: `param_lst` = a "list" of ten elements:doall = nil to do active dwg only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic connector; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil")dlst = (ignore if doall = nil). nil = process all drawings in project, otherwise do this list of drawings in the current project: (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_connpin_rpt1.html`.
- `c:wd_connpin_rpt2` - Cause "Connector pin - Details" report generation without any user prompts. Signature: `(c:wd_connpin_rpt2 param_lst)`. Parameters: `param_lst` = a "list" of ten elements:doall = nil to do active dwg only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic connector symbols; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil")dlst = (ignore if doall = nil). nil = process all drawings in project, otherwise do this list of drawings in the current project: (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_connpin_rpt2.html`.
- `c:wd_frm2_r` - Cause wire or cable from/to report generation without any user prompts. Signature: `(c:wd_frm2_r param_lst)`. Parameters: `param_lst` = A "list" of twelve elements:xtr = 1 to force a fresh extract of wire information, 0 = reuse wire extract data from the last run.when xtr = 0 then type and doall values are ignored.type = 0 = Wire from/to, 1 = Cable conductor from/to.options = nil if none, 1's bit set = Include reverse sequences in the output when use of "wire sequencing" results in a from/to wire connection that is the reverse of what is given in "frmlst" and "tolst".doall = nil to do current drawing only, 1 = all in project, or (list "path/dwg1" "path/dwg2" ...) to process a specific list of drawings.frmlst = "list" of "from" location codes; for example, (list "PNL1" "PNL2"). Wild cards are permitted. To enable all location codes, use (list "*").tolst = same as frmlst but "to" location codes.fut2 = (future) set to nil.format = output file format "ASC" for ascii, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data, "XLS" for Excel, "MDB" for Microsoft Access.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP variables: for AutoCAD Release 14 it is simply FNAM, for AutoCAD 2000 the report file name can be retrieved via the AutoLISP expression (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_frm2_r.html`.
- `c:wd_get_schem_tags_in_use` - Each returned value is a sublist consisting of Tag-ID, INST, LOC, and DESC1-3 values. Returned list is sorted by Tag-ID and then by LOC. Signature: `(c:wd_get_schem_tags_in_use options)`. Parameters: `options` = 1 to process current dwg only, 2 = project-wide, 3 = proj-wide with "Freshen".. Returns: (list (list tagname inst loc d1 d2 d3)(list tagname inst loc d1 d2 d3)(...)) Source: `c_wd_get_schem_tags_in_use.html`.
- `c:wd_getlocs` - For the active drawing only, this routine extracts directly from the entities currently in the drawing. For a project-wide list, this routine queries the active project's database file, tables "COMP", "PLCIO", and "TERM" for schematic and "PNLCOMP", "PNLTERM" for panel. This routine does NOT refresh the database before doing the query. You may want to call (c:wd_mdb_freshen nil) if you're not sure whether the database file is fully in synch with the drawing set. Signature: `(c:wd_getlocs param_lst)`. Parameters: `param_lst` = A list of four elements: doall = 1 to process all drawings in project, nil to process current drawing only.what = "LOC", "INST", "MOUNT", or "GROUPWITH".filter = 1 for Schem elements only, 2 = Panel only, 3 = Schem and Panel.outfilename = output text file name, nil = don't output list to a file.. Returns: List of codes. Source: `c_wd_getlocs.html`.
- `c:wd_mark_get_initials` - Prompt for user initials and comments to be used in wd_mark_get_initials function parameters. Signature: `(c:wd_mark_get_initials)`. Returns: "cancel_flag" = nil if user DIDN'T cancel, = 1 if user did cancel. Source: `c_wd_mark_get_initials.html`.
- `c:wd_mark_proj` - Cause project-wide mark/verify with/without user prompts. Signature: `(c:wd_mark_proj param_lst)`. Parameters: `param_lst` = A "list" of seven elements:dowhat = 0 for MARK mode, 1 for VERIFY mode, 2 to REMOVE mark info.mark_log = log file name, ""=defaults to "wdmark".initials = MARK mode: user initials to be saved and reported on VERIFY.comment1 = MARK mode: user comment1 reported on VERIFY.comment2 = MARK mode: user comment1 reported on VERIFY.comment3 = MARK mode: user comment1 reported on VERIFY.options = 1's bit do all proj dwgs set without prompting user to select dwgs, 2's bit set for MARK mode mark non- AutoCAD Electrical block inserts, 4's bit set for MARK mode: mark line wire entities, 8's bit set for VERIFY mode: suppress display of report at end.. Example topic: `_c_wd_mark_proj_3_Example.html`. Source: `c_wd_mark_proj.html`.
- `c:wd_pjconn_r` - Cause schematic plug-jack connection report generation without any user prompts. Signature: `(c:wd_pjconn_r param_lst)`. Parameters: `param_lst` = A "list" of ten elements:doall = nil to do active drawing only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic connector symbols; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil").dlst = (ignore if doall = nil). nil = do all drawings in project, otherwise do this list of dwgs in the current project (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).category = WDTYPE filter (nil or "" = schematic, "1-" = one-line, etc.). Optional.. Source: `c_wd_pjconn_r.html`.
- `c:wd_term_nums_rpt` - Cause schematic terminal numbers report generation without any user prompts. Signature: `(c:wd_term_nums_rpt param_lst)`. Parameters: `param_lst` = a "list" of nine elements:doall = nil to do active dwg only, 1 = process active project (see dlst parameter element).todo_locs = 1 to process all schematic terminal symbols; 2 process only those that have a blank location code; 3 process those subject to wild card match with "doloc" parameter.doloc = location code to include (if todo_locs = 3). Can have wild cards per AutoCAD standards (for example, "*JBOX*" means any location with "JBOX" substring).incl = (not used, set to "nil")dlst = (ignore if doall = nil). nil = process all drawings in project, otherwise do this list of drawings in the current project: (list "dwg1" "dwg2" ...). Full path not needed.format = output file format "ASC" for ascii, "XLS" for Excel, "MDB" for Access, "CSV" for comma delimited ascii, "CSV+P" for comma delimited with project information as first line of data.fields = 0 to output all fields; 1 = output only fields defined in default ".set" report settings file; = specific settings file to use (".set" extension is not required, use double backslashes if path is given).outfilename = output file name, use single forward or double backslashes in path.scriptfilename = optional script file name (executes after report extraction is complete). The "outfilename" can be accessed from within the script file through the following AutoLISP expression: (vl-bb-ref 'FNAM).. Source: `c_wd_term_nums_rpt.html`.

### Section J - User Table

User table tools.

- Source topic: `Section_J_-_User_Table.html`
- Routine count: 7

- `c:wd_user_del` - Delete user record number from an entity. Signature: `(c:wd_user_del ben recnum)`. Parameters: `ben` = Entity name or entity handle number.; `recnum` = Record number (integer or three-digit text string value, ex: "005").. Returns: 1 = success, nil = not found. Source: `c_wd_user_del.html`.
- `c:wd_user_delall` - Delete all existing user records from an entity. Signature: `(c:wd_user_delall ben)`. Parameters: `ben` = Entity name or entity handle number.. Returns: Count (integer)of number of records deleted, nil = none found. Source: `c_wd_user_delall.html`.
- `c:wd_user_get` - Get user record data from an entity. Signature: `(c:wd_user_get ben recnum)`. Parameters: `ben` = Entity name or entity handle number.; `recnum` = Record number (integer or three-digit text string value, ex: "005").. Returns: Text value returned (255 char max) or nil if record not present on the target entity. Source: `c_wd_user_get.html`.
- `c:wd_user_getall` - Get all user record data from an entity. Signature: `(c:wd_user_getall ben dataonly)`. Parameters: `ben` = Entity name or entity handle number.; `dataonly` = 1 to return a list of the data, nil = return data in the form of a list of lists where each sublist is a list of the recnum and datavalue (for example, (list ("001" "Rec value one")("002" "Rec value two")...). Returns: Data list (or list of lists), nil = no data found. Source: `c_wd_user_getall.html`.
- `c:wd_user_getnext` - Get next user record from an entity. Signature: `(c:wd_user_getnext ben start)`. Parameters: `ben` = Entity name or entity handle number.; `start` = Starting record number (integer or 3-char string). Value of "000" or 0 or nil means start at the first or only record on the entity.. Returns: Text value returned or nil if no more records found on entity. Source: `c_wd_user_getnext.html`.
- `c:wd_user_put` - If the given recnum already exists, overwrite the old value with the newvalue. Signature: `(c:wd_user_put ben recnum newval)`. Parameters: `ben` = Entity name or entity handle number.; `recnum` = Record number (integer or three-digit text string value, ex: "005").; `newval` = Text string data (255 char max) to insert on to the entity.. Returns: 1 = success, nil = failure. Source: `c_wd_user_put.html`.
- `c:wd_user_putnext` - Put user record data on to an entity using the next unused rec number. Signature: `(c:wd_user_putnext ben start newval)`. Parameters: `ben` = Entity name or entity handle number.; `start` = Starting record number (integer or 3-char string). Value of "000" or 0 or nil means start at the first or only record on the entity.; `newval` = Text string data (255 char max) to insert on to the entity.. Returns: Returns 3-char recnum code used, nil = failure. Source: `c_wd_user_putnext.html`.

### Section K - XLS and MDB Tools

Microsoft Access/Excel file read/write tools.

- Source topic: `Section_K_-_XLS_and_MDB_Tools.html`
- Routine count: 16

- `wd_mdb_AddRecs` - Add records to given table. Signature: `(wd_mdb_AddRecs hdl tabnam reclst)`. Parameters: `hdl` = File handle of opened file.; `tabnam` = Table name (for Excel, use "Sheet1$").; `reclst` = (list (list (list fieldname1 fieldval)(list fieldname2 fieldval)â€¦) (list â€¦)). Returns: T = success, nil = failure. Example topic: `_wd_mdb_AddRecs_3_Example.html`. Source: `wd_mdb_AddRecs.html`.
- `wd_mdb_Close` - Close opened file. Signature: `(wd_mdb_Close hdl)`. Parameters: `hdl` = File handle of opened file.. Returns: T = file closed, nil = failure or file was not open or invalid handle. Source: `wd_mdb_Close.html`.
- `wd_mdb_CloseAll` - Close all opened mdb files. Signature: `(wd_mdb_CloseAll)`. Returns: T = success, nil = failure or no files open. Source: `wd_mdb_CloseAll.html`.
- `wd_mdb_Create` - Create new mdb file, overwrite any existing file. Signature: `(wd_mdb_Create hdl fnam)`. Parameters: `hdl` = File handle returned by (wd_mdb_GetHandle 1).; `fnam` = mdb filename to create.. Returns: T = success, nil = failure. Source: `wd_mdb_Create.html`.
- `wd_mdb_CreateTable` - Create new table in opened or newly created mdb file, overwriting existing. Signature: `(wd_mdb_CreateTable hdl tabnam field_lst x)`. Parameters: `hdl` = File handle returned by (wd_mdb_GetHandle 1).; `tabnam` = Table name to create.; `field_lst` = Table's field definition (list (list name typ width)(list â€¦)) where: name = field name.typ = "S" for character, "A" for autonumber.width = character width (use 0 for type "A").; `x` = 1. Returns: T = success, nil = failure. Source: `wd_mdb_CreateTable.html`.
- `wd_mdb_DeleteRecs` - Delete target records. Signature: `(wd_mdb_DeleteRecs hdl tabnam query)`. Parameters: `hdl` = File handle of opened file.; `tabnam` = Table name (for Excel, use "Sheet1$").; `query` = Query to specify target records.. Returns: T = success, nil = failure. Source: `wd_mdb_DeleteRecords.html`.
- `wd_mdb_GetFieldSpecs` - Return list of field names, field type, field width values of specified table name. Signature: `(wd_mdb_GetFieldSpecs hdl tabnam)`. Parameters: `hdl` = File handle of opened file.; `tabnam` = Name of table to query.. Returns: (list (list fieldnam1 typ width)(fieldnam2 typ width)â€¦) Source: `wd_mdb_GetFieldSpecs.html`.
- `wd_mdb_GetHandle` - Get MS Access/Excel file handle. Signature: `(wd_mdb_GetHandle typ)`. Parameters: `typ` = 1 for Access format (mdb), 2 for Excel format (xls).. Returns: File handle for opening a file found to be in Microsoft Access format, nil = failure. Source: `wd_mdb_GetHandle.html`.
- `wd_mdb_GetRecCount` - Return number of records in table. Signature: `(wd_mdb_GetRecCount hdl tabnam)`. Parameters: `hdl` = File handle of opened file.; `tabnam` = Name of table.. Source: `wd_mdb_GetRecCount.html`.
- `wd_mdb_GetRecs` - Query target table and return matching records. Signature: `(wd_mdb_GetRecs hdl tabnam fields2rtrn query sortorder)`. Parameters: `hdl` = File handle of opened mdb file.; `tabnam` = Name of table to query.; `fields2rtrn` = "" to return all fields or "fieldnam1,fieldnam2..." to return specific fields.; `query` = "" to return all records, non-blank to return records matching specific query.; `sortorder` = "" to return records in natural order or "fieldnam1,fieldnam2..." to return records sorted by the given fields, left to right.. Returns: (list (list rec_data) (list rec_data) ... ) Source: `wd_mdb_GetRecs.html`.
- `wd_mdb_GetTabNames` - Return table names found in opened file. Signature: `(wd_mdb_GetTabNames hdl)`. Parameters: `hdl` = File handle of opened mdb file.. Returns: (list tablename1 tablename2â€¦) or nil if failure. Example topic: `_wd_mdb_GetTabNames_3_Example.html`. Source: `wd_mdb_GetTabNames.html`.
- `wd_mdb_ModifyFieldData` - Modify contents of one or more fields in the target record. Signature: `(wd_mdb_ModifyFieldData hdl tabnam query recdata)`. Parameters: `hdl` = File handle of opened file.; `tabnam` = Table name (for Excel, use "Sheet1$").; `query` = Query to specify target record (for example, use string "CATALOG = '010344' AND STYLE = 'RED'" to specify target record with catalog field value '010344' and style field value 'RED').; `recdata` = (list (list fieldname1 fieldval)(list fieldname2 fieldval)â€¦). Returns: T = success, nil = failure. Source: `wd_mdb_ModifyFieldData.html`.
- `wd_mdb_Open` - Open Excel or Access file for read/modify. Signature: `(wd_mdb_Open hdl fnam mode)`. Parameters: `hdl` = File handle returned by (wd_mdb_GetHandle 1).; `fnam` = xls or mdb filename to open.; `mode` = (Excel only, not required for Access) Example: Excel 5.0;HDR=YES. Returns: 1 = success, nil = failure. Example topic: `_wd_mdb_Open_3_Example.html`. Source: `wd_mdb_Open.html`.
- `wd_oledb_command` - Perform an SQL-type command on database file. Signature: `(wd_oledb_command fnam query_str)`. Parameters: `fnam` = Name of database file.; `query_str` = SQL command.. Example topic: `_wd_oledb_command_3_Example.html`. Source: `wd_oledb_command.html`.
- `wd_oledb_delete` - Delete selected records from table in database file. Signature: `(wd_oledb_delete fnam table query_str)`. Parameters: `fnam` = Name of database file.; `table` = Name of table from which to delete records.; `query_str` = SQL select to identify records to delete. NOTE! If this argument is omitted, then ALL records are deleted.. Source: `wd_oledb_delete.html`.
- `wd_oledb_select` - Perform an SQL-type query on database file. Signature: `(wd_oledb_select fnam query_str)`. Parameters: `fnam` = Name of database file.; `query_str` = SQL query.. Example topic: `_wd_oledb_select_3_Example.html`. Source: `wd_oledb_select.html`.

### Section L - Circuit Builder

Circuit Builder specific API calls.

- Source topic: `Section_L_-_Circuit_Builder.html`
- Routine count: 23

- `c:ace_cb_adjust_wire_crossings` - Process wire found at location "xy" and adjust any wire crossings to match active drawing's wire crossing graphics setting (nth 49 GBL_wd_m). Signature: `(c:ace_cb_adjust_wire_crossings xy options)`. Parameters: `xy` = nil = use coordinate of current marker block being processedcoordinate = process underlying wire at this coordinatenon-blank string = process all marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed; `options` = future. Source: `c_ace_cb_adjust_wire_crossings.html`.
- `c:ace_cb_anno` - This data passed as argument "data" which references a global (held in memory) list of data lists. This function does not process the first two sublists in this global data structure but starts at the third list (index=2) where catalog assignment information begins. Data structure is illustrated below. If target catalog assignment and/or RATINGx attributes are present and non-blank values are present in the "data" global variable, then the values will annotate on to the component's attributes. Signature: `(c:ace_cb_anno data options)`. Parameters: `data` = global (held in memory) data structure passed from circuit builder's 2nd ARX GUI. If this argument set to nil then it will default to using global "data" if exists.; `options` = 1's bit set= don't hide repeated RATING1 on child polesclear= hide repeated RATING1 values on child poles2's bit set (and 1's bit clear)= show last RATING1, hide the rest clear (and 1's bit clear)=show first RATING1, hide the rest4's bit set=multi-catalog RATINGx text values inserted "as defined"clear= multi-catalog RATING flag increment to next higher unused RATINGx attrib.For example, both main and 1st multi-cat entries have non-blank RATING1=xx values. The main goes to RATING1, the multi-cat goes to RATING2 (ex: for a fused-disconnect switch, RATING1 for switch amps and the 2nd catalog number for a fuse gets pushed up to RATING2)8's bit set= main part number copied to child poles if they carry MFG/CAT attributesclear= main part number does NOT copy to child poles16's bit set= any extra part number copied to child polesclear= extra part number does NOT copy to child poles. Example topic: `_c_ace_cb_anno_3_Example.html`. Source: `c_ace_cb_anno.html`.
- `c:ace_cb_anno2` - Signature: `(c:ace_cb_anno2 data2 code target options param_list)`. Parameters: `data2` = global (held in memory) data structure passed from circuit builder's 2nd ARX GUI. If this argument set to nil then it will default to using global "data2" if exists.; `code` = The "Ax" identifier, used to find a specific sublist in the "data2" structure.; `target` = The target attribute name to push value out to. If blank, default to RATING1.; `options` = 1's bit set= don't hide repeated RATING1 on child polesclear= hide repeated RATING1 values on child poles2's bit set (and 1's bit clear)= show last RATING1, hide the rest clear (and 1's bit clear)=show first RATING1, hide the rest4's bit set=multi-catalog RATINGx text values inserted "as defined"clear= multi-catalog RATING flag increment to next higher unused RATINGx attrib.For example, both main and 1st multi-cat entries have non-blank RATING1=xx values. The main goes to RATING1, the multi-cat goes to RATING2 (ex: for a fused-disconnect switch, RATING1 for switch amps and the 2nd catalog number for a fuse gets pushed up to RATING2)8's bit set= main part number copied to child poles if they carry MFG/CAT attributesclear= main part number does NOT copy to child poles16's bit set= any extra part number copied to child polesclear= extra part number does NOT copy to child poles; `param_lst` = nil (future). Source: `c_ace_cb_anno2.html`.
- `c:ace_cb_attr` - Push attribute value "atval" out to attribute name "atnam" on block insert that is carried as first element in global #hdl_list Signature: `(c:ace_cb_attr atnam atval formatstr options)`. Parameters: `atnam` = The attribute name to look for. Wild cards are acceptable.; `atval` = The attribute value to write out. If it is an expression, then it needs to be passed as a string with double-quotes. Example: "(nth 1 (nth 0 #data))"; `formatstr` = nil for none, otherwise "atval" substituted into %N part of format str.Example: "atval"="460", format str= "%N VAC", atval result= "460 VAC"; `options` = 1's bit set = prepend "atval" to any existing value found on the attribute (atval is the prefix)2's bit set = append "atval" to any existing value found on the attribute (atval becomes suffix)4's bit set = overwrite existing value even if "atval" is blank bit clear means preserve any existing value if "atval" is blank (i.e. do not overwrite existing value with a blank value)8's bit set = write to attribute or out to Xdata if attribute not present. Source: `c_ace_cb_attr.html`.
- `c:ace_cb_catalog_lookup` - Open the catalog lookup dialog box. Signature: `(c:ace_cb_catalog_lookup table_code mfg cat asmb options)`. Parameters: `table_code` = table name like "SS" or "PB" or "TRMS" or "PLCIO" mfg cat asmb; `mfg` = manufacturer code (or nil); `cat` = catalog part number (or nil); `asmb` = ASSYCODE value (or nil); `options` = 1's bit set = return is list of lists including any subassy entries ; nil or 1's bit not set = return is one list of the main part number. Source: `c_ace_cb_catalog_lookup.html`.
- `c:ace_cb_eval` - This function can be encoded into the Circuit Builder spreadsheet and greatly expands its ability to encode full lisp expressions into the spreadsheet. Signature: `(c:ace_cb_eval str)`. Parameters: `str` = Lisp expression. Source: `c_ace_cb_eval.html`.
- `c:ace_cb_figure_mccdb_fnam` - Internal Circuit Builder use. Look for ace_electrical_standards.mdb file to use for the current project. 1st choice= ace_electrical_standards.mdb in same subdir as project's wdp file 2nd choice= ace_electrical_standards.mdb in USER subdir 3rd choice= ace_electrical_standards.mdb in catalogs subdir 4th choice= ace_electrical_standards.mdb in ACAD paths Signature: `(c:ace_cb_figure_mccdb_fnam)`. Returns: Full path name to target file. nil=failure. Source: `c_ace_cb_figure_mccdb_fnam.html`.
- `c:ace_cb_get` - Retrieve "codename" attribute value or component handle number previously saved by a call to (c:ace_cb_save...) in global GBL_wd_cb_save_@list@ during the current or last circuit build. This global is reset at the start of the Circuit Build command. This means that it is still in memory after a circuit is completed (and before a new one is started) and may be accessed by some post-processing application. Signature: `(c:ace_cb_get codename options)`. Parameters: `codename` = Alias codename (case-sensitive) value associated with target attribute value (for example: "@MOTOR_NUM@"); `options` = 1 to return component handle, anything else to return saved value. Source: `c_ace_cb_get.html`.
- `c:ace_cb_insert` - Invoke Circuit Builder and insert complete circuit without any user prompts. Signature: `(c:ace_cb_insert param_lst)`. Parameters: `param_lst` = (list xy template_dwg xls_fnam xls_sheet insckt_scl symscl rungvertdist runghorizdist history_name options annoSpecifics)where"xy" = main template insertion coordinate, nil=user prompted to pick insertion point"template_dwg" = main template dwg to insert; ".DWG" extension is not required; Normal AutoCAD Electrical and ACAD paths are searched if the full path is not provided."xls_fnam" = circuit builder XLS file, nil = default to "ace_circuit_builder.xls" Normal AutoCAD Electrical and ACAD paths are searched if the full path is not provided."xls_sheet" = code sheet that corresponds to the selected template"insckt_scl" = main template insertion scale, nil = use drawing default"symscl" = component insertion scale, nil = use current default"rungvertdist" = spacing adjustment distance between marked horiz wires"runghorizdist" = spacing adjustment distance between marked vertical wires"history_name" = nil if no "history" reference, otherwise = previously assigned circuit name"options" = 1's bit set=debug messages on, nil=no options selected"annoSpecifics" = optional annotation specifics, in the format of list of list of "marker code", "attribute name" and "attribute value" to be applied to the inserted circuit.. Source: `c_ace_cb_insert.html`.
- `c:ace_cb_insym` - Insert component, break underlying wires. Signature: `(c:ace_cb_insym xy future blknam scl options anno_string)`. Parameters: `xy` = nil = use coordinate of current marker block being processed; coordinate = trim underlying wire at this; non- blank string = trim user under marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed; `future` = nil (future use); `blknam` = block name to insert. Can include full path. Does not require ".dwg" extension.; `scl` = insertion scale; nil = follows (nth 5 GBL_wd_cb_data), and if nil then defaults to value of global GBL_wd_scl or 1.0; `options` = 1's bit set = open INS/EDIT dialog box after insertion2's bit set = return "handle" of inserted symbol (otherwise return ent name)4's bit set = when 1's bit = 0, suppress parent component or terminal tag auto-generation8's bit set = allow motor angled connections to go to non-wire LINE entity (used by CIRC BLDR)"H" (or 2 or 8) = horizontal, "V" (or 1 or 4) =vertical; `anno_string` = optional annotation to assign attribute values and/or lisp expressions to execute. Separate each value with a semi-colon. For example:DESC1=MOTOR;RATING5=30A. Returns: nil if fail, otherwise return entity name or handle of inserted component Source: `c_ace_cb_insym.html`.
- `c:ace_cb_insym_DynamicBlock` - Inserts dynamic block component and adjusts to hit the marked wire connections. Signature: `(c:ace_cb_insym_DynamicBlock xy future blknam scl options anno_string)`. Parameters: `xy` = nil = use coordinate of current marker block being processedcoordinate = trim underlying wire at thisnon-blank string = trim user under marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed.; `future` = nil (future use); `blknam` = The block name to insert. Can include full path. Does not require ".dwg" extension.; `scl` = insertion scale, nil = follows (nth 5 GBL_wd_cb_data), and if nil then defaults to value of global GBL_wd_scl or 1.0; `options` = 1's bit set = display INS/EDIT dialog after insertion2's bit set = return "handle" of inserted symbol (otherwise return ent name)4's bit set = when 1's bit =0, suppress parent component or terminal tag auto-generation8's bit set = allow motor angled connections to go to non-wire LINE entity (used by CIRC BLDR); `anno_string` = optional annotation to assign attribute values and/or lisp expressions to execute. Separate each value with a semi-colon. For example:DESC1=MOTOR;RATING5=30A. Returns: nil if fail, otherwise returns entity name or handle of inserted component. Source: `c_ace_cb_insym_DynamicBlock.html`.
- `c:ace_cb_insym_signal` - Inserts a "signal" arrow symbol at the marker block location. Signature: `(c:ace_cb_insym_signal xy future blknam scl options anno_string)`. Parameters: `xy` = nil = use coordinate of current marker block being processedcoordinate = trim underlying wire at thisnon-blank string = trim user under marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed.; `future` = nil (future use); `blknam` = block name to insert (nil=auto-calculate block name). Can include full path. Does not require ".dwg" extension.example: "HA1S1"; `scl` = insertion scale, nil = follows (nth 5 GBL_wd_cb_data), and if nil then defaults to value of global GBL_wd_scl or 1.0; `options` = 1's bit set = display INS/EDIT dialog after insertion2's bit set = return "handle" of inserted symbol (otherwise return ent name)4's bit set = when 1's bit =0, suppress parent component or terminal tag auto-generation; `anno_string` = optional annotation attribute= (ex: "@SIGCODE@") - value to insert and/or lisp expressions to execute. Source: `c_ace_cb_insym_signal.html`.
- `c:ace_cb_multipole` - Insert mulit-pole component, break underlying wires. Signature: `(c:ace_cb_multipole xy default_dir blknam_lst polecnt scl options anno_string)`. Parameters: `default_dir` = build direction 8 = build down, 2 = build up, 1 = build left to right, 4 = build right to leftnil = build down if horiz wire, right if vertical wire. If the specific marker block that triggers this call carries a "_D=[num]" entry on its MISC1 attribute, then this MISC1 value overrides any passed "default_dir" value.4's bit set = allow connections to LINEs that are not (yet) on a valid wire layer; `blknam_lst` = parent symbol name, or (list parent_blknam child_blknam) = list of parent name and child name or child_blknam if inserting multipole child component; `polecnt` = total number of poles to insert; `scl` = nil should always be passed as the value. For change in symbol insertion scale use GBL_wd_symscl.; `options` = 1's bit set = opens INS/EDIT dialog box after insertion2's bit set = return "handle" of inserted symbol (otherwise return ent name)4's bit set = allow connections to LINEs that are not (yet) on a valid wire layeranno_str nil for none, otherwise list of attrib = value pairs and/or other override flags.Can also carry optional tag format (ex: "MTR-%N") to apply to the multi-pole component. If nil, then defaults to drawing default. If the specific marker block that triggers this call carries a MISC1 value with a tag format value, then the marker block's value overrides both this argument value and the drawing default tag format value.; `anno_string` = optional annotation to assign attribute values and/or lisp expressions to execute. Separate each value with a semi-colon. For example:DESC1=MOTOR;RATING5=30A. Returns: nil if fail, otherwise returns list of entity names or handles of inserted poles, in order. This list also held in AutoLISP global #hdl_list. Source: `c_ace_cb_multipole.html`.
- `c:ace_cb_nested` - Circuit Builder use. Insert nested circuit "fnam" at location "xy". Circuit is inserted "exploded". Scale is pulled from (nth 1 GBL_wd_cb_data) global or defaults to 1.0 if global not found. Signature: `(c:ace_cb_nested fnam xyz options_lst)`. Parameters: `fnam` = File name of nested circuit drawing. ".DWG" extension is not required. Normal AutoCAD Electrical and ACAD paths are searched if full path not provided.; `xyz` = = nil = use coordinate of current marker block being processed= coordinate = insert connector at this XY coordinate= non-blank string = insert at marker block(s) that are marked with this string; `options_lst` = future. Source: `c_ace_cb_nested.html`.
- `c:ace_cb_parametric_connector` - Insert parametrically generated connector component. Signature: `(c:ace_cb_parametric_connector xy direction flip pincnt pintext spacing wn_chg scl other_options_lst anno_string)`. Parameters: `direction` = nil = align with underlying wire, 1 = vertical insert, 2 = horizontal insert; `flip` = 1 = plug side on right or on top, 2 = receptacle side on right or on top; `pincnt` = number of connector pins; `pintext` = first pin number value or comma-delimited list, nil or blank = start with "1"; `spacing` = 1 = fixed pin spacing, 2 = adjust to underlying wire spacing; `wn_chg` = 1 = wire number changes across connector, 0 = no change through connector; `scl` = nil = default scale (follows global GBL_wd_scl_plcbld); `other_options_lst` = future; `anno_string` = nil for none, otherwise list of attrib = value pairs and/or other override flags. Returns: nil if fail; otherwise list of entity names of connector or connector pieces that inserted Source: `c_ace_cb_parametric_connector.html`.
- `c:ace_cb_rung_spacing` - Retrieve horizontal and vertical bus spacing defaults from (nth 4 GBL_wd_cb_data) and (nth 3 GBL_wd_cb_data) respectively. Finds circuit builder marker block CODE/ORDER value pair being processed from this global: (nth (car GBL_wd_cb_marker_data) (cadr GBL_wd_cb_marker_data)) and then all other marker blocks in the group by match on this CODE/ORDER combination. Signature: `(c:ace_cb_rung_spacing anno_str)`. Parameters: `anno_str` = future. Source: `c_ace_cb_rung_spacing.html`.
- `c:ace_cb_save` - Extract attribute value, pair it with an alias codename value and handle value, and hold in memory for possible retrieval later by call to (c:ace_cb_get...). Codename/Value pairs are held in memory in an AutoLISP global "GBL_wd_cb_save_@list@". The structure of this global is (list (list codename attrval hdl)(list codename attrval hdl)...) Signature: `(c:ace_cb_save codename attrname ent_lst options)`. Parameters: `codename` = An alias flag to pair with the attribute value. Can be any text string.Example: "@MOTOR_NUM@"; `attrname` = Attribute name to search for. Can include wildcards. Example: "TAG1*" or "TAG1*,TAG2,TAGSTRIP,P_TAG1"; `ent_lst` = List of entities to process (i.e. look for attribute "attrname" and get value) nil then start from entity held in GBL_wd_cb_markerblock_start_entlast and work until hit end of database; `options` = 1's bit set = extract numeric substring (ex: return "250" if attrval = "M-250")If no numeric part found, then just return the whole attribute value string.. Source: `c_ace_cb_save.html`.
- `c:ace_cb_save_for_link` - Use in the spreadsheet right after one of the insert Circuit Builder APIs. Blocks grouped together for linking are linked with the dashed link line after the entire circuit is built." Signature: `(c:ace_cb_save_for_link grp place)`. Parameters: `grp` = to allow you to define any number of groups of blocks to link within the circuit. Integer.; `place` = the order within the group to link together. Integer.. Source: `c_ace_cb_save_for_link.html`.
- `c:ace_cb_save_selection` - Push circuit selection data out to the main motor or load component of the circuit being processed. This data allows the circuit to be edited to re-display / re-calculate wire sizes or referenced during another circuit insert (example: reference an existing one-line circuit when inserting an equivalent three-phase schematic circuit). Signature: `(c:ace_cb_save_selection hdl #data callback2 options)`. Parameters: `hdl` = Handle target component, nil=use global #hdl_list to determine current component being processed. #data : nil = use default #data global, if it exists. This motor/load and wire size data is a list of two lists. An example of the first part of this #data list is shown here:("Induction" "25" "HP" "208" "3" "60" "1200" "76" "1.25" "0" "AWG" "CU" "THWN" "75C" "0" "" "" "0" "0" "0.8" "" "0" "" "0" "0.8" "" "" "0.08" "6AWG" "!MCC_CTRL")This first part is formatted and pushed on to an VIA_WD_CB_CIRCPARAMS Xdata value on the "hdl" motor or load symbol.An example of the second part of the #data list is shown here:(("1AWG" "BRN_1AWG" 1) ("1AWG" "BLK_1AWG" 1) ("1AWG" "GRY_1AWG" 1) ("6AWG" "" 3))This second part, if it exists in the list, is pushed into an VIA_WD_CB_CIRCPARAMS2 Xdata value on the "hdl" motor or load symbol.If no data, then the Xdata tags are not created. #data : nil = use default #data global, if it exists.This includes the data that is formatted and pushed on to VIA_WD_CB_CIRCPARAMS Xdata value. If no data, then the Xdata tag is not created.; `callback2` = nil = use the default found in (car GBL_wd_cb_select_circuit_callback) and(cadr GBL_wd_cb_select_circuit_callback).This includes the data that is formatted and pushed onto the VIA_WD_CB_CIRCSELECT Xdata value.; `options` = nil. Source: `c_ace_cb_save_selection.html`.
- `c:ace_cb_set_wiretype` - Extract wire size from global (i.e. held in memory) list of data and format an appropriate wire type layer name. Signature: `(c:ace_cb_set_wiretype data motor_wire_index options misc1_spreadsheet)`. Parameters: `data` = global (held in memory) data structure passed from circuit builder's 2nd ARX GUI. If this argument set to nil then it will default to using global "data" if exists. This "data" is a list of mutliple lists with this function accessing only the second list of the overall list, that is, "(nth 1 #data) or (cadr #data)". This second entry consists of multiple sublists, each sublist tied to a conductor feeding the circuit's main load (ex: motor). These sublists are in this format: (list size_substr default_laynam flag) where"size_substr" = wire size pulled from ace_electrical_standards.mdb"default_laynam" = the default wire layer name for the marked conductor"flag" = internal useThe size_substr value can be accessed through alias codename "@WSIZE@" by either a MISC1 attribute value on the marker block or as the value for argument "misc1_spreadsheet" encoded in the spreadsheet entry. The format is preceeded by a "_WIRETYPE=" prefix, example, "_WIRETYPE=BRN-@WSIZE@-AWG".; `motor_wire_index` = Position ("1" based) in (cadr _#data) for wire type data. If nil or blank then it defaults to the first conductor entry in the list if no override found on marker block or carried in the marker block's spreadsheet entry.; `options` = 1's bit set = if new layer created, do not mark it as a "wire" layer.2's bit set = filter out entries in (cadr _#data) for wire type data. Only process those entries with the 2's bit set in the last element of each wiretype sublist.Example:(cadr _#data) = (("1AWG" "BRN_1AWG" 1) ("1AWG" "BLK_1AWG" 1)("1AWG" "GRY_1AWG" 1) ("6AWG" "GRN_6AWG" 3)))... means filter out all but the last entry. This is used to find the "ground" wiretype from the list of passed wire types.2's bit clear = don't filter out entries in (cadr _#data). Process the list "as passed."4's bit set = process only marked wire segment.4's bit clear = process marked "network."; `misc1_spreadsheet` = Any string value embedded in this API call's third argument as shown in the spreadsheet.. Source: `c_ace_cb_set_wiretype.html`.
- `c:ace_cb_stretch_wire_connect` - Find wire under "xy" point and try to stretch to connect to nearest crossing wire or component wire connection point. Signature: `(c:ace_cb_stretch_wire_connect xy options misc1_spreadsheet)`. Parameters: `xy` = nil = use coordinate of current marker block being processed.coordinate = stretch underlying wire found at this XY coordinate.non-blank string = trim user under marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed.; `options` = 1's bit set=suppress stretched wire from matching layer of any wire it connects to; `misc1_spreadsheet` = any string value embedded in this API call's second argument as shown in the spreadsheet. This value is overridden by any MISC1 attribute value carried on the marker block. The attribute value pulled from a global variable (if exists).. Source: `c_ace_cb_stretch_wire_connect.html`.
- `c:ace_cb_trim` - Trim wire found at location "xy". Signature: `(c:ace_cb_trim xy options)`. Parameters: `xy` = nil = use coordinate of current marker block being processed.Coordinate = trim underlying wire at this.Non-blank string = trim user under marker block(s) that are marked with this string as their CODE attribute value (wild card match) AND have same ORDER value as the main marker block being processed.; `options` = 1's bit set = don't trim away any IEC-tee symbols at end of trimmed wire2's bit set = don't trim past any gap/loop, leave loop in place if exists. Source: `c_ace_cb_trim.html`.
- `c:ace_cb_wnum` - Generate and insert a wire number on target wire network line segment "wen" or on line segment under coordinate "passed_xy". Signature: `(c:ace_cb_wnum passed_xy wen options anno_str)`. Parameters: `passed_xy` = XY position on wire segment; nil = use "wen" argument; `wen` = entity name of wire segment of network; nil = use "passed_xy" coordinate to find wire segment; `options` = 1's bit set = insert FIXED wire number2's bit set = return "handle" of inserted wire number (otherwise return ent name)4's bit set = attempt to position new wire number at the passed xy coordinatebit clear = new wire number inserts at network default position; `anno_str` = nil for none; otherwise list of attrib = value pairs and/or other override flags. Returns: nil if fail, otherwise returns entity name (or handle) of inserted wire number Source: `c_ace_cb_wnum.html`.

### Section M - Project Database Service

The Project Database Service can be accessed through Visual LISP.

- Source topic: `Section_M_-_Project_Database_Service.html`
- Routine count: 2

- `LISP` - Source: `LISP.html`.
- `Appendix` - Source: `Appendix.html`.

## Samples Index

- Source topic: `Samples_Index.html`

### General Usage Samples

- `ObjectARX` - Sample to map a Japanese color suffix to each wire number based upon the name of the wire's LINE layer name. - Topic: `ObjectARX_code_samples.html`.
- `AutoLISP` - Sample to map a Japanese color suffix to each wire number based upon the name of the wire's LINE layer name. - Topic: `AutoLISP_sample_code.html`.
- `Visual Basic` - Sample to map a Japanese color suffix to each wire number based upon the name of the wire's LINE layer name. - Topic: `Visual_Basic_sample_code.html`.
- `AutoLISP` - Sample called from Title Block Update command. - Topic: `AutoLISP_code_samples_(PLC).html`.
- `AutoLISP` - Sample to track the drawing "Section" assignment and format title block text. - Topic: `AutoLISP_code_samples_(PLC).html`.
- `AutoLISP` - Auto-run a utility when drawing opens from Project Manager or "Next/Previous" arrow picks. - Topic: `AutoLISP_code_samples_(PLC).html`.
- `AutoLISP` - Sample called from Insert PLC command. - Topic: `AutoLISP_code_samples_(PLC).html`.
- `AutoLISP` - Invoking API calls from a redefined function. - Topic: `AutoLISP_code_samples_(PLC).html`.

### Reference Sample Topics

#### Section A. Insert Component, Break Wire, and Auto Tag Generation.

- `wd_get_pinlist` - Topic: `c_wd_get_pinlist.html`.
- `wd_ins_circ2` - Topic: `c_wd_ins_circ2.html`.
#### Section C. Insert wires and wire numbers.

- `ace_get_wnum` - Topic: `c_ace_get_wnum.html`.
- `wd_get_wire_netlst` - Topic: `c_wd_get_wire_netlst.html`.
- `wd_putwnf` - Topic: `c_wd_putwnf.html`.
#### Section D. PLC functions.

- `wdio_autorun` - Topic: `c_wdio_autorun.html`.
#### Section E. Insert and edit ladders. Drawing setup. Title block.

- `wd_chg_ldr_info` - Topic: `c_wd_chg_ldr_info.html`.
#### Section F. General Functions.

- `ace_get_cat_data` - Topic: `c_ace_get_cat_data.html`.
- `ace_getattr_data` - Topic: `c_ace_getattr_data.html`.
- `wd_2ss` - Topic: `c_wd_2ss.html`.
- `wd_bswap` - Topic: `c_wd_bswap.html`.
- `wd_getattrval` - Topic: `c_wd_getattrval.html`.
- `wd_selsort` - Topic: `c_wd_selsort.html`.
#### Section G. Project / project database file.

- `wd_is_cur_dwg_in_proj` - Topic: `c_wd_is_cur_dwg_in_proj.html`.
- `wd_mdb_get_proj_scratch_dbnam` - Topic: `c_wd_mdb_get_proj_scratch_dbnam.html`.
- `wd_proj_wdp_data` - Topic: `c_wd_proj_wdp_data.html`.
#### Section J. Microsoft Access/Excel file read/write tools.

- `wd_mdb_addrecs` - Topic: `wd_mdb_AddRecs.html`.
- `wd_mdb_gettabnames` - Topic: `wd_mdb_GetTabNames.html`.
- `wd_mdb_open` - Topic: `wd_mdb_Open.html`.
