# Archive

Retired components kept for reference. These were replaced during the workspace overhaul.

- **dashboard/** -- DashboardShell, DashboardHeader, DashboardSidebar, etc. Replaced by WorkspaceShell layout system.
- **ai-old/** -- Original Ollama-based AI chat components. Replaced by unified AI system in `components/ai-unified/`.
- **ai-new/** -- The 5 AI-SDK chat files that were merged into the unified AI system.
- **memory-map/** -- Standalone AI Memory Map. Merged into the unified graph visualization.
- **architecture/** -- ArchitectureMapPanel, Inspectors, 2D index. Merged into unified graph.
- **apps/** -- AppsHub, AdvancedFeatures. Replaced by ContextPanel and route-based navigation.
- **storage/** -- StorageManager monolith, FileUploadModal. Split into modular components in `components/storage/`.
- **misc/** -- Dead code that was never imported (DesignPreviewModal, ThreeBackground, etc.).
