# Block Library Route Notes

The live Block Library route now lives at
`src/routes/apps/block-library/BlockLibraryRoutePage.tsx`.

This folder is retained only as a lightweight historical note so older path
references do not imply the app is still a placeholder.
