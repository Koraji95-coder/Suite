# Post-Overhaul Feature Backlog

This note captures the immediate backlog split after the project-setup/title-block cutover tranche. It is a planning bucket list, not a canonical runtime architecture spec.

## Now

- Batch find and replace (`mixed`)
- Link Data (`mixed`)
- Crosswires (`mixed`)

## Later

- Drawing List Manager enhancements (`mixed`)
- Dimension sets (`CAD`)
- Express Auto Align (`CAD`)
- time-saved graphs/reporting (`hosted core`)

## Explore

- Earthplex lite (`CAD`)
- shared app-library triage / unknown Autodesk links / ProView identification (`mixed`)
- APS Viewer / hyperlink-review exploration (`web`)
- Scikit-learn or PyTorch assistive scoring (`hosted core`)

## Skip

- AI CAD GPT as a primary lane (`mixed`)
- Tinkercad as a core direction (`web`)
- legacy Transmittal Builder as a roadmap item (`mixed`; keep only as reference)
